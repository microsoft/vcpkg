diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1990f5793..7baad1b88 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -14,7 +14,7 @@ set(CMAKE_FIND_PACKAGE_PREFER_CONFIG ON)
 set(minBoostVersion 1.65)
 set(minQtVersion 6.0.2)
 set(minOpenSSLVersion 1.1.1)
-set(minLibtorrentVersion 1.2.13)
+set(minLibtorrentVersion 1.2.12) # use 1.2.12 just to make things easier because vcpkg does not have 1.2.13 yet and this does not affect anything related to Qt anyway
 set(minZlibVersion 1.2.11)
 
 # features (some are platform-specific)
diff --git a/src/app/CMakeLists.txt b/src/app/CMakeLists.txt
index 6c0eac4d6..b87231bc5 100644
--- a/src/app/CMakeLists.txt
+++ b/src/app/CMakeLists.txt
@@ -142,7 +142,7 @@ endif()
 
 if (GUI)
     target_link_libraries(qbt_app PRIVATE qbt_gui)
-    if ((CMAKE_SYSTEM_NAME STREQUAL "Windows") OR (CMAKE_SYSTEM_NAME STREQUAL "Darwin"))
+    if (((CMAKE_SYSTEM_NAME STREQUAL "Windows") AND (NOT MSVC_RUNTIME_DYNAMIC)) OR (CMAKE_SYSTEM_NAME STREQUAL "Darwin"))
         qt_import_plugins(qbt_app INCLUDE Qt6::QSvgIconPlugin Qt6::QSvgPlugin)
     endif()
 endif()
