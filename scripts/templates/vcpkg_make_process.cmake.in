if (@SELECT_TRIPLETS@)
    vcpkg_configure_make(
        SOURCE_PATH ${SOURCE_PATH}
        #AUTOCONFIG
        #SKIP_CONFIGURE
        #NO_DEBUG
        #AUTO_HOST
        #AUTO_DST
        #PRERUN_SHELL ${SHELL_PATH}
        #OPTIONS
        #OPTIONS_DEBUG
        #OPTIONS_RELEASE
    )
    
    vcpkg_install_make()
endif()