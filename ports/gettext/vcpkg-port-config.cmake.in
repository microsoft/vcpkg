get_filename_component(gettext_tools_dir "${CMAKE_CURRENT_LIST_DIR}/../../../@HOST_TRIPLET@/tools/gettext/bin" ABSOLUTE)
if(CMAKE_HOST_WIN32)
    set(ENV{PATH} "$ENV{PATH};${gettext_tools_dir}")
elseif(CMAKE_HOST_UNIX)
    set(ENV{PATH} "$ENV{PATH}:${gettext_tools_dir}")
endif()

