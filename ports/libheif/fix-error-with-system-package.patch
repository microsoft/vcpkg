diff --git a/gdk-pixbuf/CMakeLists.txt b/gdk-pixbuf/CMakeLists.txt
index 892cee4..a226641 100644
--- a/gdk-pixbuf/CMakeLists.txt
+++ b/gdk-pixbuf/CMakeLists.txt
@@ -1,7 +1,14 @@
 if(UNIX OR MINGW)
   find_package(PkgConfig)
   find_package(Threads)
-  pkg_check_modules(GDKPIXBUF2 gdk-pixbuf-2.0 IMPORTED_TARGET)
+  
+  if(CMAKE_BUILD_TYPE STREQUAL "Debug")
+	set(ENV{PKG_CONFIG_PATH} "${_VCPKG_INSTALLED_DIR}/${VCPKG_TARGET_TRIPLET}/debug/lib/pkgconfig")
+  else()
+	set(ENV{PKG_CONFIG_PATH} "${_VCPKG_INSTALLED_DIR}/${VCPKG_TARGET_TRIPLET}/lib/pkgconfig")
+  endif()
+  
+  pkg_check_modules(GDKPIXBUF2 NO_CMAKE_PATH gdk-pixbuf-2.0 IMPORTED_TARGET)
 
   if(GDKPIXBUF2_FOUND)
     pkg_get_variable(GDKPIXBUF2_MODULE_DIR gdk-pixbuf-2.0 gdk_pixbuf_moduledir)
