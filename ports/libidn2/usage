The package @PORT@:@TARGET_TRIPLET@ provides CMake targets:

    find_package(libidn2 REQUIRED)
    target_include_directories(main PRIVATE ${LIBIDN2_INCLUDE_DIRS})
    target_link_libraries(main PRIVATE ${LIBIDN2_LIBRARIES})
