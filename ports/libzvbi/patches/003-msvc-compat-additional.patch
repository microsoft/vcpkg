diff --git a/Makefile.am b/Makefile.am
index 69c876d..c511abe 100644
--- a/Makefile.am
+++ b/Makefile.am
@@ -20,8 +20,7 @@ DISTCLEANFILES = site_def.h
 SUBDIRS = \
 	m4 \
 	src \
-	$(proxy_dirs) \
-	contrib
+	$(proxy_dirs)
 
 if ENABLE_EXAMPLES
 SUBDIRS += examples
diff --git a/configure.ac b/configure.ac
index 7dc6650..a928571 100644
--- a/configure.ac
+++ b/configure.ac
@@ -539,7 +539,6 @@ AM_CONDITIONAL(ENABLE_EXAMPLES, [test "x$enable_examples" = xyes])
 
 AC_CONFIG_FILES([
   Makefile
-  contrib/Makefile
   examples/Makefile
   daemon/Makefile
   daemon/zvbid.init
diff --git a/src/export.c b/src/export.c
index c2b61a9..e6fa743 100644
--- a/src/export.c
+++ b/src/export.c
@@ -46,6 +46,27 @@
 #include <io.h>
 #endif
 
+#ifdef _MSC_VER
+#  include <limits.h>
+#  ifndef SSIZE_MAX
+#    define SSIZE_MAX INTPTR_MAX
+#  endif
+#  ifndef S_IRUSR
+#    define S_IRUSR _S_IREAD
+#    define S_IWUSR _S_IWRITE
+#    define S_IRGRP 0
+#    define S_IWGRP 0
+#    define S_IROTH 0
+#    define S_IWOTH 0
+#  endif
+#  ifndef S_ISREG
+#    define S_ISREG(m) (((m) & _S_IFMT) == _S_IFREG)
+#  endif
+#  ifndef __PRETTY_FUNCTION__
+#    define __PRETTY_FUNCTION__ __FUNCSIG__
+#  endif
+#endif
+
 #include "export.h"
 #include "conv.h"
 #include "vbi.h" /* asprintf */
diff --git a/src/inout.c b/src/inout.c
index f31bd81..96d4e55 100644
--- a/src/inout.c
+++ b/src/inout.c
@@ -462,7 +462,7 @@ vbi_capture_delete(vbi_capture *capture)
 		capture->_delete(capture);
 }
 
-static __inline__ void
+static inline void
 timeval_subtract		(struct timeval *	delta,
 				 const struct timeval *	tv1,
 				 const struct timeval *	tv2)
diff --git a/src/io-sim.c b/src/io-sim.c
index 45c05a6..38f5780 100644
--- a/src/io-sim.c
+++ b/src/io-sim.c
@@ -25,6 +25,9 @@
 #  include "config.h"
 #endif
 
+#ifdef _MSC_VER
+#  define _USE_MATH_DEFINES
+#endif
 #include <math.h>		/* sin(), log() */
 #include <errno.h>
 #include <ctype.h>		/* isspace() */
diff --git a/src/teletext.c b/src/teletext.c
index 8d342b0..dedb038 100644
--- a/src/teletext.c
+++ b/src/teletext.c
@@ -246,7 +246,7 @@ top_label(vbi_decoder *vbi, vbi_page *pg, struct vbi_font_descr *font,
 	return FALSE;
 }
 
-static __inline__ vbi_pgno
+static inline vbi_pgno
 add_modulo			(vbi_pgno		pgno,
 				 int			incr)
 {
