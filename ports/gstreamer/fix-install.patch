diff --git a/subprojects/gst-devtools/meson.build b/subprojects/gst-devtools/meson.build
index 4201ded..0cc017e 100644
--- a/subprojects/gst-devtools/meson.build
+++ b/subprojects/gst-devtools/meson.build
@@ -147,13 +147,13 @@ foreach extra_arg : warning_flags
 endforeach
 
 pkgconfig = import('pkgconfig')
-plugins_install_dir = join_paths(get_option('libdir'), 'gstreamer-1.0')
+plugins_install_dir = join_paths(get_option('libdir'), '')
 plugins_pkgconfig_install_dir = join_paths(plugins_install_dir, 'pkgconfig')
 if get_option('default_library') == 'shared'
   # If we don't build static plugins there is no need to generate pc files
   plugins_pkgconfig_install_dir = disabler()
 endif
-pkgconfig_subdirs = ['gstreamer-1.0']
+pkgconfig_subdirs = []
 
 plugins_doc_dep = []
 plugins = []
diff --git a/subprojects/gst-plugins-bad/meson.build b/subprojects/gst-plugins-bad/meson.build
index f5c10c4..70cd0ce 100644
--- a/subprojects/gst-plugins-bad/meson.build
+++ b/subprojects/gst-plugins-bad/meson.build
@@ -34,7 +34,7 @@ curversion = gst_version_minor * 100 + gst_version_micro
 libversion = '@0@.@1@.0'.format(soversion, curversion)
 osxversion = curversion + 1
 
-plugins_install_dir = join_paths(get_option('libdir'), 'gstreamer-1.0')
+plugins_install_dir = join_paths(get_option('libdir'), '')
 static_build = get_option('default_library') == 'static'
 plugins = []
 libraries = []
@@ -482,7 +482,7 @@ pkgconfig_variables = ['exec_prefix=${prefix}',
     'girdir=${datadir}/gir-1.0',
     'typelibdir=${libdir}/girepository-1.0']
 
-pkgconfig_subdirs = ['gstreamer-1.0']
+pkgconfig_subdirs = []
 
 pkgconfig.generate(
   libraries : [gst_dep],
diff --git a/subprojects/gst-plugins-base/meson.build b/subprojects/gst-plugins-base/meson.build
index adf94ad..25f5955 100644
--- a/subprojects/gst-plugins-base/meson.build
+++ b/subprojects/gst-plugins-base/meson.build
@@ -44,7 +44,7 @@ curversion = gst_version_minor * 100 + gst_version_micro
 libversion = '@0@.@1@.0'.format(soversion, curversion)
 osxversion = curversion + 1
 
-plugins_install_dir = join_paths(get_option('libdir'), 'gstreamer-1.0')
+plugins_install_dir = join_paths(get_option('libdir'), '')
 static_build = get_option('default_library') == 'static'
 plugins = []
 libraries = []
@@ -453,7 +453,7 @@ pkgconfig_variables = ['exec_prefix=${prefix}',
     'girdir=${datadir}/gir-1.0',
     'typelibdir=${libdir}/girepository-1.0',
     'libexecdir=${prefix}/libexec']
-pkgconfig_subdirs = ['gstreamer-1.0']
+pkgconfig_subdirs = []
 
 meson_pkg_config_file_fixup_script = find_program('scripts/meson-pkg-config-file-fixup.py')
 
diff --git a/subprojects/gst-plugins-good/meson.build b/subprojects/gst-plugins-good/meson.build
index af74c80..3b66974 100644
--- a/subprojects/gst-plugins-good/meson.build
+++ b/subprojects/gst-plugins-good/meson.build
@@ -30,7 +30,7 @@ endif
 
 api_version = '1.0'
 
-plugins_install_dir = join_paths(get_option('libdir'), 'gstreamer-1.0')
+plugins_install_dir = join_paths(get_option('libdir'), '')
 plugins = []
 
 cc = meson.get_compiler('c')
diff --git a/subprojects/gst-plugins-ugly/meson.build b/subprojects/gst-plugins-ugly/meson.build
index 97ef9d0..bf5cf5b 100644
--- a/subprojects/gst-plugins-ugly/meson.build
+++ b/subprojects/gst-plugins-ugly/meson.build
@@ -29,7 +29,7 @@ endif
 
 api_version = '1.0'
 
-plugins_install_dir = join_paths(get_option('libdir'), 'gstreamer-1.0')
+plugins_install_dir = join_paths(get_option('libdir'), '')
 plugins = []
 
 cc = meson.get_compiler('c')
diff --git a/subprojects/gst-rtsp-server/meson.build b/subprojects/gst-rtsp-server/meson.build
index f0d2f64..4bc83de 100644
--- a/subprojects/gst-rtsp-server/meson.build
+++ b/subprojects/gst-rtsp-server/meson.build
@@ -192,7 +192,7 @@ if get_option('default_library') == 'shared'
 endif
 
 plugins = []
-pkgconfig_subdirs = ['gstreamer-1.0']
+pkgconfig_subdirs = []
 static_build = get_option('default_library') == 'static'
 libraries = []
 
diff --git a/subprojects/gstreamer/meson.build b/subprojects/gstreamer/meson.build
index 65d26c2..e7d3ff2 100644
--- a/subprojects/gstreamer/meson.build
+++ b/subprojects/gstreamer/meson.build
@@ -30,7 +30,7 @@ prefix = get_option('prefix')
 
 datadir = join_paths(prefix, get_option('datadir'))
 libexecdir = get_option('libexecdir')
-helpers_install_dir = join_paths(libexecdir, 'gstreamer-1.0')
+helpers_install_dir = join_paths(libexecdir, '')
 
 cc = meson.get_compiler('c')
 
@@ -117,7 +117,7 @@ cdata.set_quoted('PACKAGE_TARNAME', 'gstreamer')
 cdata.set_quoted('PACKAGE_BUGREPORT', 'https://gitlab.freedesktop.org/gstreamer/gstreamer/issues/new')
 cdata.set_quoted('PACKAGE_URL', '')
 cdata.set_quoted('PACKAGE_VERSION', gst_version)
-cdata.set_quoted('PLUGINDIR', join_paths(get_option('prefix'), get_option('libdir'), 'gstreamer-1.0'))
+cdata.set_quoted('PLUGINDIR', join_paths(get_option('prefix'), get_option('libdir'), ''))
 cdata.set_quoted('VERSION', gst_version)
 cdata.set_quoted('GST_PLUGIN_SCANNER_INSTALLED', join_paths(prefix, helpers_install_dir, 'gst-plugin-scanner'))
 cdata.set_quoted('GST_PTP_HELPER_INSTALLED', join_paths(prefix, helpers_install_dir, 'gst-ptp-helper'))
@@ -562,7 +562,7 @@ if bashcomp_dep.found()
   endif
 endif
 
-plugins_install_dir = join_paths(get_option('libdir'), 'gstreamer-1.0')
+plugins_install_dir = join_paths(get_option('libdir'), '')
 
 pkgconfig = import('pkgconfig')
 plugins_pkgconfig_install_dir = join_paths(plugins_install_dir, 'pkgconfig')
@@ -583,7 +583,7 @@ pkgconfig_uninstalled_variables = ['exec_prefix=${prefix}',
 	'gstreamerdir=${prefix}/subprojects/gstreamer',
     'bashhelpersdir=${gstreamerdir}/data/bash-completion/helpers',
     'helpersdir=${gstreamerdir}/libs/gst/helpers']
-pkgconfig_subdirs = ['gstreamer-1.0']
+pkgconfig_subdirs = []
 
 static_build = get_option('default_library') == 'static'
 libraries = []
