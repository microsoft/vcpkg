diff --git a/CMakeLists.txt b/CMakeLists.txt
index cd04b9745..691e15e46 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -447,6 +447,9 @@ add_library(freetype
   ${BASE_SRCS}
 )
 
+if(EMSCRIPTEN)
+  target_compile_options(freetype PUBLIC -mbulk-memory -matomics)
+endif()
 set_target_properties(
   freetype PROPERTIES
     C_VISIBILITY_PRESET hidden)
