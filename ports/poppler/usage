The package poppler can be imported via CMake FindPkgConfig module:

    find_package(unofficial-poppler CONFIG REQUIRED)
    target_link_libraries(main PRIVATE unofficial::poppler::poppler-cpp unofficial::poppler::poppler-private)
