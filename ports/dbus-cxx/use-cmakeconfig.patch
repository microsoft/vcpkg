diff --git a/CMakeLists.txt b/CMakeLists.txt
index 0030f17..e6c8fb7 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -36,7 +36,7 @@ set( DBUS_CXX_INCLUDE_VERSION 2.0 )
 set( PKG_VERSION ${dbus-cxx_VERSION} )
 
 # Our required dependencies: libsigc++ 3.0
-pkg_check_modules( sigc REQUIRED IMPORTED_TARGET sigc++-3.0 )
+find_package(sigc++-3 CONFIG REQUIRED)
 
 #
 # Check our options
@@ -239,7 +239,7 @@ include_directories( ${DBUS_CXX_INCLUDE_DIRECTORIES} )
 #
 set( THREADS_PREFER_PTHREAD_FLAG ON )
 find_package( Threads REQUIRED )
-set( DBUS_CXX_LIBRARIES PkgConfig::sigc Threads::Threads )
+set( DBUS_CXX_LIBRARIES sigc-3.0 Threads::Threads )
 
 find_library( LIBRT rt )
 if( LIBRT )
@@ -355,18 +355,26 @@ INSTALL( FILES "${PROJECT_BINARY_DIR}/dbus-cxx-2.0.pc"
 # Support for find_package
 #
 install(EXPORT dbus-cxxTargets
-  FILE        dbus-cxxConfig.cmake
-  NAMESPACE     dbus-cxx::
+  FILE        dbus-cxxTargets.cmake
+  NAMESPACE   dbus-cxx::
   DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx
 )
 
 include(CMakePackageConfigHelpers)
-write_basic_package_version_file("${CMAKE_CURRENT_BINARY_DIR}/dbus-cxxConfigVersion.cmake"
+write_basic_package_version_file(
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxxConfigVersion.cmake"
   VERSION       ${PROJECT_VERSION}
   COMPATIBILITY SameMajorVersion
 )
-install(FILES   "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxxConfigVersion.cmake"
-  DESTINATION   ${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx
+configure_package_config_file(
+  "${PROJECT_SOURCE_DIR}/cmake/dbus-cxxConfig.cmake.in"
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxxConfig.cmake"
+  INSTALL_DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx"
+)
+install(FILES
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxxConfig.cmake"
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxxConfigVersion.cmake"
+  DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx"
 )
 
 #
diff --git a/dbus-cxx-glib/CMakeLists.txt b/dbus-cxx-glib/CMakeLists.txt
index 6b98f27..4d679b7 100644
--- a/dbus-cxx-glib/CMakeLists.txt
+++ b/dbus-cxx-glib/CMakeLists.txt
@@ -22,7 +22,7 @@ set_target_properties( dbus-cxx-glib PROPERTIES
     VERSION 2.0.0 SOVERSION 2
     PUBLIC_HEADER "${dbus-cxx-glib-headers}"
 )
-target_link_libraries( dbus-cxx-glib PUBLIC dbus-cxx PkgConfig::sigc ${LIBRT} )
+target_link_libraries( dbus-cxx-glib PUBLIC dbus-cxx sigc-3.0 ${LIBRT} )
 target_include_directories( dbus-cxx-glib INTERFACE
     $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
     $<INSTALL_INTERFACE:include/dbus-cxx-glib-${DBUS_CXX_INCLUDE_VERSION}>
@@ -65,8 +65,30 @@ INSTALL( FILES "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-glib-2.0.pc"
 # Library install information
 #
 install( TARGETS dbus-cxx-glib
-    EXPORT dbus-cxxTargets
+    EXPORT dbus-cxx-glibTargets
     COMPONENT glib
     PUBLIC_HEADER DESTINATION include/dbus-cxx-glib-${DBUS_CXX_INCLUDE_VERSION}
     LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}" )
+
+install(EXPORT dbus-cxx-glibTargets
+  FILE        dbus-cxx-glibTargets.cmake
+  NAMESPACE   dbus-cxx::
+  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-glib
+)
+include(CMakePackageConfigHelpers)
+write_basic_package_version_file(
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-glibConfigVersion.cmake"
+  VERSION       ${PROJECT_VERSION}
+  COMPATIBILITY SameMajorVersion
+)
+configure_package_config_file(
+  "${PROJECT_SOURCE_DIR}/cmake/dbus-cxx-glibConfig.cmake.in"
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-glibConfig.cmake"
+  INSTALL_DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-glib"
+)
+install(FILES
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-glibConfig.cmake"
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-glibConfigVersion.cmake"
+  DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-glib"
+)
diff --git a/dbus-cxx-qt/CMakeLists.txt b/dbus-cxx-qt/CMakeLists.txt
index 9c637ed..12f5ab4 100644
--- a/dbus-cxx-qt/CMakeLists.txt
+++ b/dbus-cxx-qt/CMakeLists.txt
@@ -28,7 +28,7 @@ set_target_properties( dbus-cxx-qt PROPERTIES
     VERSION 2.0.0 SOVERSION 2
     PUBLIC_HEADER "${dbus-cxx-qt-headers}"
 )
-target_link_libraries( dbus-cxx-qt PUBLIC Qt5::Core dbus-cxx PkgConfig::sigc ${LIBRT} )
+target_link_libraries( dbus-cxx-qt PUBLIC Qt5::Core dbus-cxx sigc-3.0 ${LIBRT} )
 target_include_directories( dbus-cxx-qt INTERFACE
     $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
     $<INSTALL_INTERFACE:include/dbus-cxx-qt-${DBUS_CXX_INCLUDE_VERSION}>
@@ -71,8 +71,30 @@ INSTALL( FILES "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-qt-2.0.pc"
 # Library install information
 #
 install( TARGETS dbus-cxx-qt
-    EXPORT dbus-cxxTargets
+    EXPORT dbus-cxx-qtTargets
     COMPONENT qt
     PUBLIC_HEADER DESTINATION include/dbus-cxx-qt-${DBUS_CXX_INCLUDE_VERSION}
     LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}" )
+
+install(EXPORT dbus-cxx-qtTargets
+  FILE        dbus-cxx-qtTargets.cmake
+  NAMESPACE   dbus-cxx::
+  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-qt
+)
+include(CMakePackageConfigHelpers)
+write_basic_package_version_file(
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-qtConfigVersion.cmake"
+  VERSION       ${PROJECT_VERSION}
+  COMPATIBILITY SameMajorVersion
+)
+configure_package_config_file(
+  "${PROJECT_SOURCE_DIR}/cmake/dbus-cxx-qtConfig.cmake.in"
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-qtConfig.cmake"
+  INSTALL_DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-qt"
+)
+install(FILES
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-qtConfig.cmake"
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-qtConfigVersion.cmake"
+  DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-qt"
+)
diff --git a/dbus-cxx-uv/CMakeLists.txt b/dbus-cxx-uv/CMakeLists.txt
index 23d51c5..1a4e795 100644
--- a/dbus-cxx-uv/CMakeLists.txt
+++ b/dbus-cxx-uv/CMakeLists.txt
@@ -18,7 +18,7 @@ else()
   set( LIBUV_PKG_NAME libuv )
 endif()
 
-pkg_check_modules( libuv REQUIRED IMPORTED_TARGET ${LIBUV_PKG_NAME} )
+find_package(libuv CONFIG REQUIRED)
 
 set( dbus-cxx-uv-headers dbus-cxx-uv.h uvdispatcher.h )
 set( dbus-cxx-uv-sources dbus-cxx-uv.cpp uvdispatcher.cpp )
@@ -28,12 +28,12 @@ set_target_properties( dbus-cxx-uv PROPERTIES
     VERSION 2.0.0 SOVERSION 2
     PUBLIC_HEADER "${dbus-cxx-uv-headers}"
 )
-target_link_libraries( dbus-cxx-uv PUBLIC dbus-cxx PkgConfig::sigc ${LIBRT} )
+target_link_libraries( dbus-cxx-uv PUBLIC dbus-cxx sigc-3.0 ${LIBRT} )
 target_include_directories( dbus-cxx-uv INTERFACE
     $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}>
     $<INSTALL_INTERFACE:include/dbus-cxx-uv-${DBUS_CXX_INCLUDE_VERSION}>
 )
-target_link_libraries( dbus-cxx-uv PUBLIC PkgConfig::libuv )
+target_link_libraries( dbus-cxx-uv PUBLIC $<IF:$<TARGET_EXISTS:libuv::uv_a>,libuv::uv_a,libuv::uv> )
 
 set_property( TARGET dbus-cxx-uv PROPERTY CXX_STANDARD 17 )
 
@@ -72,8 +72,30 @@ INSTALL( FILES "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-uv-2.0.pc"
 # Library install information
 #
 install( TARGETS dbus-cxx-uv
-    EXPORT dbus-cxxTargets
+    EXPORT dbus-cxx-uvTargets
     COMPONENT uv
     PUBLIC_HEADER DESTINATION include/dbus-cxx-uv-${DBUS_CXX_INCLUDE_VERSION}
     LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
     ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}" )
+
+install(EXPORT dbus-cxx-uvTargets
+  FILE        dbus-cxx-uvTargets.cmake
+  NAMESPACE   dbus-cxx::
+  DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-uv
+)
+include(CMakePackageConfigHelpers)
+write_basic_package_version_file(
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-uvConfigVersion.cmake"
+  VERSION       ${PROJECT_VERSION}
+  COMPATIBILITY SameMajorVersion
+)
+configure_package_config_file(
+  "${PROJECT_SOURCE_DIR}/cmake/dbus-cxx-uvConfig.cmake.in"
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-uvConfig.cmake"
+  INSTALL_DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-uv"
+)
+install(FILES
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-uvConfig.cmake"
+  "${CMAKE_CURRENT_BINARY_DIR}/dbus-cxx-uvConfigVersion.cmake"
+  DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/dbus-cxx-uv"
+)
