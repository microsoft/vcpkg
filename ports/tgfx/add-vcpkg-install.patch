diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -618,0 +619,35 @@ endif ()
+
+# Install targets for vcpkg
+set(TGFX_INSTALL_TARGETS tgfx)
+if(TARGET tgfx-drawers)
+    list(APPEND TGFX_INSTALL_TARGETS tgfx-drawers)
+endif()
+
+install(TARGETS ${TGFX_INSTALL_TARGETS}
+    EXPORT tgfxTargets
+    LIBRARY DESTINATION lib
+    ARCHIVE DESTINATION lib
+    RUNTIME DESTINATION bin
+    INCLUDES DESTINATION include
+)
+
+install(DIRECTORY include/
+    DESTINATION include
+    FILES_MATCHING PATTERN "*.h" PATTERN "*.hpp"
+)
+
+if(TGFX_BUILD_DRAWERS AND TARGET tgfx-drawers)
+    install(DIRECTORY drawers/include/
+        DESTINATION include
+        FILES_MATCHING PATTERN "*.h" PATTERN "*.hpp"
+    )
+endif()
+
+install(EXPORT tgfxTargets
+    FILE tgfxTargets.cmake
+    NAMESPACE tgfx::
+    DESTINATION share/tgfx
+)
+
+file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/tgfxConfig.cmake" "include(CMakeFindDependencyMacro)\ninclude(\${CMAKE_CURRENT_LIST_DIR}/tgfxTargets.cmake)\nset(tgfx_FOUND TRUE)\n")
+install(FILES "${CMAKE_CURRENT_BINARY_DIR}/tgfxConfig.cmake" DESTINATION share/tgfx)
+