diff --git a/CMakeLists.txt b/CMakeLists.txt
index ad7ae72..3bb3e2f 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -92,6 +92,11 @@ target_link_libraries(ripe
     PUBLIC cryptopp::cryptopp
     ${ZLIB_LIBRARIES}
 )
+if(MSVC)
+    target_link_options(ripe PRIVATE
+        /INCREMENTAL:NO
+    )
+endif()
 
 target_compile_definitions(ripe PRIVATE
     ELPP_NO_DEFAULT_LOG_FILE
@@ -137,6 +142,14 @@ set_target_properties (ripe-bin PROPERTIES
     VERSION ${RIPE_VERSION}
     SOVERSION ${RIPE_SOVERSION}
 )
+if(MSVC)
+    set_target_properties(ripe-bin PROPERTIES
+        PDB_NAME "ripe-bin"
+    )
+    target_link_options(ripe-bin PRIVATE
+        /INCREMENTAL:NO
+    )
+endif()
 install (TARGETS ripe-bin DESTINATION bin)
 
 ############################################################################################
