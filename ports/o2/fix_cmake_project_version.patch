diff --git a/CMakeLists.txt b/CMakeLists.txt
index c95f678..84851e4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -4,7 +4,7 @@ set( CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/cmake/modules )
 
 set(VER_MAJOR "1")
 set(VER_MINOR "0")
-set(VER_PATCH "1")
+set(VER_PATCH "2")
 set(API_VER_PATCH "0")
 set(CPACK_PACKAGE_VERSION_MAJOR ${VER_MAJOR})
 set(CPACK_PACKAGE_VERSION_MINOR ${VER_MINOR})
@@ -12,7 +12,9 @@ set(CPACK_PACKAGE_VERSION_PATCH ${VER_PATCH})
 set(PROJECT_VERSION ${VER_MAJOR}.${VER_MINOR}.${VER_PATCH})
 set(API_VERSION ${VER_MAJOR}.${VER_MINOR}.${API_VER_PATCH})
 
-project(o2)
+project(o2 VERSION ${PROJECT_VERSION})
+
+
 
 if(NOT QT_DEFAULT_MAJOR_VERSION)
     set(QT_DEFAULT_MAJOR_VERSION 5 CACHE STRING "Qt version to use (4, 5 or 6), defaults to 5")
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index fcac7b8..8a17a88 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -246,6 +246,7 @@ if(BUILD_SHARED_LIBS)
           PROPERTIES INSTALL_NAME_DIR ${CMAKE_INSTALL_PREFIX}/lib
       )
     endif(APPLE)
+    message(STATUS ${API_VERSION})
     set_target_properties(o2
         PROPERTIES VERSION ${PROJECT_VERSION} SOVERSION ${API_VERSION}
     )
