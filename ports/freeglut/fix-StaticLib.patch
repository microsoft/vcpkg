diff --git a/CMakeLists.txt b/CMakeLists.txt
index 42914c8..6900427 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -405,7 +405,12 @@ IF(FREEGLUT_BUILD_SHARED_LIBS)
     ENDIF()
 ENDIF()
 IF(FREEGLUT_BUILD_STATIC_LIBS)
-    ADD_LIBRARY(freeglut_static STATIC ${FREEGLUT_SRCS})
+    IF(WIN32)
+        SET(GEN_TARGET freeglut)
+    ELSE()
+        SET(GEN_TARGET freeglut_static)
+    ENDIF()
+    ADD_LIBRARY(${GEN_TARGET} STATIC ${FREEGLUT_SRCS})
 ENDIF()
 
 
@@ -421,15 +426,15 @@ IF(WIN32)
         SET_TARGET_PROPERTIES(freeglut PROPERTIES OUTPUT_NAME ${LIBNAME})
     ENDIF()
     IF(FREEGLUT_BUILD_STATIC_LIBS)
-        TARGET_COMPILE_DEFINITIONS(freeglut_static PUBLIC FREEGLUT_STATIC)
+        TARGET_COMPILE_DEFINITIONS(${GEN_TARGET} PUBLIC FREEGLUT_STATIC)
         IF(FREEGLUT_REPLACE_GLUT)
-            SET_TARGET_PROPERTIES(freeglut_static PROPERTIES OUTPUT_NAME ${LIBNAME})
+            SET_TARGET_PROPERTIES(${GEN_TARGET} PROPERTIES OUTPUT_NAME ${LIBNAME})
         ENDIF()
         # need to set machine:x64 for linker, at least for VC10, and
         # doesn't hurt for older compilers:
         # http://public.kitware.com/Bug/view.php?id=11240#c22768
         IF (CMAKE_CL_64)
-            SET_TARGET_PROPERTIES(freeglut_static PROPERTIES STATIC_LIBRARY_FLAGS "/machine:x64")
+            SET_TARGET_PROPERTIES(${GEN_TARGET} PROPERTIES STATIC_LIBRARY_FLAGS "/machine:x64")
         ENDIF()
     ENDIF()
 ELSE()
@@ -460,7 +465,7 @@ ELSE()
       SET_TARGET_PROPERTIES(freeglut PROPERTIES VERSION ${SO_MAJOR}.${SO_MINOR}.${SO_REV} SOVERSION ${SO_MAJOR} OUTPUT_NAME ${LIBNAME})
     ENDIF()
     IF(FREEGLUT_BUILD_STATIC_LIBS)
-      SET_TARGET_PROPERTIES(freeglut_static PROPERTIES OUTPUT_NAME ${LIBNAME})
+      SET_TARGET_PROPERTIES(${GEN_TARGET} PROPERTIES OUTPUT_NAME ${LIBNAME})
     ENDIF()
     IF(ANDROID)
         # Not in CMake toolchain file, because the toolchain
@@ -481,8 +486,8 @@ IF(FREEGLUT_BUILD_SHARED_LIBS)
     TARGET_COMPILE_DEFINITIONS(freeglut PUBLIC ${PUBLIC_DEFINITIONS})
 ENDIF()
 IF(FREEGLUT_BUILD_STATIC_LIBS)
-    TARGET_LINK_LIBRARIES(freeglut_static ${LIBS})
-    TARGET_COMPILE_DEFINITIONS(freeglut_static PUBLIC ${PUBLIC_DEFINITIONS})
+    TARGET_LINK_LIBRARIES(${GEN_TARGET} ${LIBS})
+    TARGET_COMPILE_DEFINITIONS(${GEN_TARGET} PUBLIC ${PUBLIC_DEFINITIONS})
 ENDIF()
 
 IF(FREEGLUT_BUILD_SHARED_LIBS)
@@ -507,7 +512,7 @@ IF(FREEGLUT_BUILD_SHARED_LIBS)
     ENDIF()
 ENDIF()
 IF(FREEGLUT_BUILD_STATIC_LIBS)
-    INSTALL(TARGETS freeglut_static EXPORT FreeGLUTTargets
+    INSTALL(TARGETS ${GEN_TARGET} EXPORT FreeGLUTTargets
             RUNTIME DESTINATION bin
             LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
             ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
@@ -538,7 +543,7 @@ MACRO(ADD_DEMO name)
         ENDIF()
         IF(FREEGLUT_BUILD_STATIC_LIBS)
             ADD_EXECUTABLE(${name}_static ${ARGN})
-            TARGET_LINK_LIBRARIES(${name}_static ${DEMO_LIBS} freeglut_static)
+            TARGET_LINK_LIBRARIES(${name}_static ${DEMO_LIBS} ${GEN_TARGET})
             IF(WIN32 AND MSVC)
                 SET_TARGET_PROPERTIES(${name}_static PROPERTIES DEBUG_POSTFIX d)
             ENDIF()
