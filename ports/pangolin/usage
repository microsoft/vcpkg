The package pangolin provides CMake integration:

    find_package(Pangolin CONFIG REQUIRED)
    target_link_libraries(main PRIVATE pangolin)
    target_include_directories(main PRIVATE ${Pangolin_INCLUDE_DIRS})
