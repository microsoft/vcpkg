diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 7f364a7..bb1574d 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -210,9 +210,14 @@ if(UNIX)
   append_glob(SOURCES utils/posix/*.cpp)
 endif()
 
+option(BUILD_PANGOLIN_PYTHON "Build support for pythonLibs" OFF)
+if (BUILD_PANGOLIN_PYTHON)
 find_package(PythonLibs QUIET)
 if(BUILD_PANGOLIN_GUI AND BUILD_PANGOLIN_VARS AND PYTHONLIBS_FOUND AND NOT _WIN_)
   set(HAVE_PYTHON 1)
+  if (UNIX)
+    set(PYTHON_LIBRARY ${PYTHON_LIBRARY} dl util)
+  endif()
   list(APPEND HEADERS
     ${INCDIR}/console/ConsoleInterpreter.h
     ${INCDIR}/console/ConsoleView.h
@@ -235,6 +240,7 @@ if(BUILD_PANGOLIN_GUI AND BUILD_PANGOLIN_VARS AND PYTHONLIBS_FOUND AND NOT _WIN_
   list(APPEND LINK_LIBS    ${PYTHON_LIBRARY})
   message(STATUS "Python Found and Enabled")
 endif()
+endif()
 
 option(BUILD_PANGOLIN_OCULUS "Build support for Oculus HUD" OFF)
 if(BUILD_PANGOLIN_OCULUS)
@@ -249,6 +255,8 @@ if(BUILD_PANGOLIN_OCULUS)
   endif()
 endif()
 
+option(BUILD_PANGOLIN_EIGEN "Build support for Eigen3" OFF)
+if (BUILD_PANGOLIN_EIGEN)
 find_package(Eigen3 QUIET)
 if(EIGEN3_FOUND)
   set(HAVE_EIGEN 1)
@@ -259,14 +267,20 @@ if(EIGEN3_FOUND)
   endif()
   message(STATUS "Eigen Found and Enabled")
 endif()
+endif()
 
+option(BUILD_PANGOLIN_Toon "Build support for Toon" OFF)
+if (BUILD_PANGOLIN_Toon)
 find_package(TooN QUIET)
 if(TooN_FOUND)
   set(HAVE_TOON 1)
   list(APPEND USER_INC  ${TooN_INCLUDE_DIR} )
   message(STATUS "TooN Found and Enabled")
 endif()
+endif()
 
+option(BUILD_PANGOLIN_DC1394 "Build support for DC1394" OFF)
+if (BUILD_PANGOLIN_DC1394)
 find_package(DC1394 QUIET)
 if(BUILD_PANGOLIN_VIDEO AND DC1394_FOUND)
   set(HAVE_DC1394 1)
@@ -276,6 +290,7 @@ if(BUILD_PANGOLIN_VIDEO AND DC1394_FOUND)
   list(APPEND SOURCES video/drivers/firewire.cpp video/drivers/firewire_deinterlace.cpp)
   message(STATUS "libdc1394 Found and Enabled")
 endif()
+endif()
 
 if(BUILD_PANGOLIN_VIDEO AND _LINUX_)
   set(HAVE_V4L 1)
@@ -284,6 +299,10 @@ if(BUILD_PANGOLIN_VIDEO AND _LINUX_)
   message(STATUS "V4L Found and Enabled")
 endif()
 
+option(BUILD_PANGOLIN_FFMPEG "Build support for ffmpeg" OFF)
+if (BUILD_PANGOLIN_FFMPEG)
+add_definitions(-DHAVE_FFMPEG_AVPIXELFORMAT)
+
 find_package(FFMPEG QUIET)
 if(BUILD_PANGOLIN_VIDEO AND FFMPEG_FOUND)
   set(HAVE_FFMPEG 1)
@@ -293,7 +312,10 @@ if(BUILD_PANGOLIN_VIDEO AND FFMPEG_FOUND)
   list(APPEND SOURCES video/drivers/ffmpeg.cpp)
   message(STATUS "ffmpeg Found and Enabled")
 endif()
+endif()
 
+option(BUILD_PANGOLIN_RealSense "Build support for RealSense" OFF)
+if (BUILD_PANGOLIN_RealSense)
 find_package(LibRealSense QUIET)
 if(BUILD_PANGOLIN_VIDEO AND LIBREALSENSE_FOUND)
   set(HAVE_LIBREALSENSE 1)
@@ -303,8 +325,11 @@ if(BUILD_PANGOLIN_VIDEO AND LIBREALSENSE_FOUND)
   list(APPEND SOURCES video/drivers/realsense.cpp)
   message(STATUS "LibRealSense Found and Enabled")
 endif()
+endif()
 
 
+option(BUILD_PANGOLIN_OpenNI "Build support for OpenNI" OFF)
+if (BUILD_PANGOLIN_OpenNI)
 find_package(OpenNI QUIET)
 if(BUILD_PANGOLIN_VIDEO AND OPENNI_FOUND)
   set(HAVE_OPENNI 1)
@@ -317,7 +342,10 @@ if(BUILD_PANGOLIN_VIDEO AND OPENNI_FOUND)
   list(APPEND SOURCES video/drivers/openni.cpp)
   message(STATUS "OpenNI Found and Enabled")
 endif()
+endif()
 
+option(BUILD_PANGOLIN_OpenNI2 "Build support for OpenNI2" OFF)
+if (BUILD_PANGOLIN_OpenNI2)
 find_package(OpenNI2 QUIET)
 if(BUILD_PANGOLIN_VIDEO AND OPENNI2_FOUND)
   set(HAVE_OPENNI2 1)
@@ -330,7 +358,10 @@ if(BUILD_PANGOLIN_VIDEO AND OPENNI2_FOUND)
   list(APPEND SOURCES video/drivers/openni2.cpp)
   message(STATUS "OpenNI2 Found and Enabled")
 endif()
+endif()
 
+option(BUILD_PANGOLIN_UVC "Build support for uvc" OFF)
+if (BUILD_PANGOLIN_UVC)
 find_package(uvc QUIET)
 if(BUILD_PANGOLIN_VIDEO AND uvc_FOUND)
   set(HAVE_UVC 1)
@@ -347,7 +378,10 @@ if(BUILD_PANGOLIN_VIDEO AND uvc_FOUND)
   endif()
   message(STATUS "libuvc Found and Enabled")
 endif()
+endif()
 
+option(BUILD_PANGOLIN_DEPTHSENSE "Build support for DepthSense" OFF)
+if (BUILD_PANGOLIN_DEPTHSENSE)
 find_package(DepthSense QUIET)
 if(BUILD_PANGOLIN_VIDEO AND DepthSense_FOUND)
   set(HAVE_DEPTHSENSE 1)
@@ -357,7 +391,10 @@ if(BUILD_PANGOLIN_VIDEO AND DepthSense_FOUND)
   list(APPEND SOURCES video/drivers/depthsense.cpp)
   message(STATUS "DepthSense Found and Enabled")
 endif()
+endif()
 
+option(BUILD_PANGOLIN_TELICAM "Build support for TeliCam" OFF)
+if (BUILD_PANGOLIN_TELICAM)
 find_package(TeliCam QUIET)
 if(BUILD_PANGOLIN_VIDEO AND TeliCam_FOUND)
   set(HAVE_TELICAM 1)
@@ -367,7 +404,10 @@ if(BUILD_PANGOLIN_VIDEO AND TeliCam_FOUND)
   list(APPEND SOURCES video/drivers/teli.cpp )
   message(STATUS "TeliCam Found and Enabled" )
 endif()
+endif()
 
+option(BUILD_PANGOLIN_PLEORA "Build support for Pleora" OFF)
+if (BUILD_PANGOLIN_PLEORA)
 find_package(Pleora QUIET)
 if(BUILD_PANGOLIN_VIDEO AND Pleora_FOUND)
   set(HAVE_PLEORA 1)
@@ -382,6 +422,7 @@ if(BUILD_PANGOLIN_VIDEO AND Pleora_FOUND)
   endif()
   message(STATUS "Pleora Found and Enabled" )
 endif()
+endif()
 
 if(NOT BUILD_EXTERN_LIBPNG)
   find_package(PNG QUIET)
@@ -404,6 +445,8 @@ if(JPEG_FOUND)
   message(STATUS "libjpeg Found and Enabled")
 endif()
 
+option(BUILD_PANGOLIN_TIFF "Build support for tiff" OFF)
+if (BUILD_PANGOLIN_TIFF)
 find_package(TIFF QUIET)
 if(TIFF_FOUND)
   set(HAVE_TIFF 1)
@@ -411,7 +454,10 @@ if(TIFF_FOUND)
   list(APPEND LINK_LIBS ${TIFF_LIBRARY} )
   message(STATUS "libtiff Found and Enabled")
 endif()
+endif()
 
+option(BUILD_PANGOLIN_OPENEXR "Build support for OpenEXR" OFF)
+if (BUILD_PANGOLIN_OPENEXR)
 find_package(OpenEXR QUIET)
 if(OpenEXR_FOUND)
   set(HAVE_OPENEXR 1)
@@ -419,6 +465,7 @@ if(OpenEXR_FOUND)
   list(APPEND LINK_LIBS ${OpenEXR_LIBRARY} )
   message(STATUS "libopenexr Found and Enabled")
 endif()
+endif()
 
 
 #######################################################
