simdissdk provides CMake targets:

    find_package(simCore CONFIG REQUIRED)
    target_link_libraries(main PRIVATE VSI::simCore)

    find_package(simData CONFIG REQUIRED)
    target_link_libraries(main PRIVATE VSI::simData)

    find_package(simDataProto CONFIG REQUIRED)
    target_link_libraries(main PRIVATE VSI::simDataProto)

    find_package(simNotify CONFIG REQUIRED)
    target_link_libraries(main PRIVATE VSI::simNotify)

    find_package(simQt CONFIG REQUIRED)
    target_link_libraries(main PRIVATE VSI::simQt)

    find_package(simUtil CONFIG REQUIRED)
    target_link_libraries(main PRIVATE VSI::simUtil)

    find_package(simVis CONFIG REQUIRED)
    target_link_libraries(main PRIVATE VSI::simVis)

    or you can use the example: ExternalSdkProject