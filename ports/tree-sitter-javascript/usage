The package tree-sitter-javascript can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_javascript REQUIRED IMPORTED_TARGET tree-sitter-javascript)

    target_link_libraries(main PRIVATE PkgConfig::ts_javascript)

