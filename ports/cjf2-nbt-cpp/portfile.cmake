vcpkg_from_github(
    OUT_SOURCE_PATH SOURCE_PATH
    REPO chenjunfu2/NBT_CPP
    REF "v${VERSION}"
    HEAD_REF master
    SHA512 6a48643331dc89cfdc6b67eff6b92754421e79f42fc4cb2545a6910fe4f003d1e54c993b8722442be308667920d4aaede384c6e61a711caa4d2523c527fd4b4e
)

file(INSTALL "${SOURCE_PATH}/include/nbt_cpp/"
     DESTINATION "${CURRENT_PACKAGES_DIR}/include/nbt_cpp"
     FILES_MATCHING
     PATTERN "*.hpp"
     PATTERN "*.h"
     PATTERN "vcpkg_config.h" EXCLUDE
)

set(CONFIG_HEADER_CONTENT "//Auto-generated by vcpkg - DO NOT EDIT\n\n")

if("zlib" IN_LIST FEATURES)
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}//use zlib\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#ifndef USE_ZLIB\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#define USE_ZLIB\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#else\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#undef USE_ZLIB\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#endif\n\n")
endif()

if("xxhash" IN_LIST FEATURES)
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}//use xxhash\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#ifndef USE_XXHASH\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#define USE_XXHASH\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#else\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#undef USE_XXHASH\n")
    set(CONFIG_HEADER_CONTENT "${CONFIG_HEADER_CONTENT}#endif\n\n")
endif()

file(WRITE "${CURRENT_PACKAGES_DIR}/include/nbt_cpp/vcpkg_config.h" "${CONFIG_HEADER_CONTENT}")

if(EXISTS "${SOURCE_PATH}/LICENSE")
    file(INSTALL "${SOURCE_PATH}/LICENSE"
         DESTINATION "${CURRENT_PACKAGES_DIR}/share/${PORT}"
         RENAME copyright)
elseif(EXISTS "${SOURCE_PATH}/LICENSE.md")
    file(INSTALL "${SOURCE_PATH}/LICENSE.md"
         DESTINATION "${CURRENT_PACKAGES_DIR}/share/${PORT}"
         RENAME copyright)
endif()