diff --git a/CMake/AbseilDll.cmake b/CMake/AbseilDll.cmake
index 47d0efd..daf699b 100644
--- a/CMake/AbseilDll.cmake
+++ b/CMake/AbseilDll.cmake
@@ -869,7 +869,7 @@ function(absl_make_dll)
     PRIVATE
       ${ABSL_DEFAULT_COPTS}
   )
-
+  if(0)
   foreach(cflag ${ABSL_CC_LIB_COPTS})
     if(${cflag} MATCHES "^(-Wno|/wd)")
       # These flags are needed to suppress warnings that might fire in our headers.
@@ -896,7 +896,7 @@ Libs: -L\${libdir} $<$<NOT:$<BOOL:${ABSL_CC_LIB_IS_INTERFACE}>>:-l${_dll}> ${PC_
 Cflags: -I\${includedir}${PC_CFLAGS}\n")
   INSTALL(FILES "${CMAKE_BINARY_DIR}/lib/pkgconfig/${_dll}.pc"
     DESTINATION "${CMAKE_INSTALL_LIBDIR}/pkgconfig")
-
+  endif()
   target_compile_definitions(
     ${_dll}
     PUBLIC
diff --git a/CMake/AbseilHelpers.cmake b/CMake/AbseilHelpers.cmake
index 61e1ae4..4ed5485 100644
--- a/CMake/AbseilHelpers.cmake
+++ b/CMake/AbseilHelpers.cmake
@@ -152,7 +152,7 @@ function(absl_cc_library)
   endif()
 
   # Generate a pkg-config file for every library:
-  if(ABSL_ENABLE_INSTALL)
+  if(0)
     if(absl_VERSION)
       set(PC_VERSION "${absl_VERSION}")
     else()
