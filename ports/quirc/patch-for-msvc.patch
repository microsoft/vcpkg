 lib/identify.c | 3 +--
 1 file changed, 1 insertion(+), 2 deletions(-)

diff --git a/lib/identify.c b/lib/identify.c
index bd9221f..b02184f 100644
--- a/lib/identify.c
+++ b/lib/identify.c
@@ -174,13 +174,12 @@ static void flood_fill_seed(struct quirc *q, int x, int y, int from, int to,
 /************************************************************************
  * Adaptive thresholding
  */
-
+#define HISTOGRAM_SIZE 256
 static uint8_t otsu(const struct quirc *q)
 {
 	int numPixels = q->w * q->h;
 
 	// Calculate histogram
-	const int HISTOGRAM_SIZE = 256;
 	unsigned int histogram[HISTOGRAM_SIZE];
 	memset(histogram, 0, (HISTOGRAM_SIZE) * sizeof(unsigned int));
 	uint8_t* ptr = q->image;
