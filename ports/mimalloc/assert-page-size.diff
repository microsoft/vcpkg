diff --git a/src/os.c b/src/os.c
index 806c51c..e37e3f4 100644
--- a/src/os.c
+++ b/src/os.c
@@ -52,6 +52,10 @@ size_t _mi_os_large_page_size(void) {
 
 size_t _mi_os_guard_page_size(void) {
   const size_t gsize = _mi_os_page_size();
+  mi_assert(gsize == 4096); /* cf. line 24 */
+  mi_assert(256 <= (MI_ARENA_SLICE_SIZE/8));
+  mi_assert(1024 <= (MI_ARENA_SLICE_SIZE/8));
+  mi_assert(2048 <= (MI_ARENA_SLICE_SIZE/8));
   mi_assert(gsize <= (MI_ARENA_SLICE_SIZE/8));
   return gsize;
 }
