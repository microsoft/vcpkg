* Kumi provides CMake targets:

    find_package(kumi CONFIG REQUIRED)
    add_executable(test_kumi your_file.cpp)
    target_link_libraries(test_kumi PUBLIC kumi::kumi)

* Using KUMI requires a C++20 compliant compiler.
