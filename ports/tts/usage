* TTS provides CMake targets:

    find_package(tts CONFIG REQUIRED)
    add_executable(test_tts your_file.cpp)
    target_link_libraries(test_tts PUBLIC tts::tts)

* Using TTS requires a C++20 compliant compiler.
