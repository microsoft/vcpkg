diff --git a/src/c++/library/CMakeLists.txt b/src/c++/library/CMakeLists.txt
--- a/src/c++/library/CMakeLists.txt
+++ b/src/c++/library/CMakeLists.txt
@@ -75,7 +75,7 @@
     target_compile_options(
       ${_json_target} PRIVATE
       $<$<OR:$<CXX_COMPILER_ID:Clang>,$<CXX_COMPILER_ID:AppleClang>,$<CXX_COMPILER_ID:GNU>>:
-        -Wall -Wextra -Werror>
+        -Wall -Wextra>
       $<$<CXX_COMPILER_ID:MSVC>:/W0 /D_WIN32_WINNT=0x0A00 /EHsc>
     )
 
@@ -141,7 +141,7 @@
     target_compile_options(
       ${_shm_target} PRIVATE
       $<$<OR:$<CXX_COMPILER_ID:Clang>,$<CXX_COMPILER_ID:AppleClang>,$<CXX_COMPILER_ID:GNU>>:
-        -Wall -Wextra -Werror>
+        -Wall -Wextra>
       $<$<CXX_COMPILER_ID:MSVC>:/W0 /D_WIN32_WINNT=0x0A00 /EHsc>
     )
 
@@ -262,7 +262,7 @@
     target_compile_options(
       ${_client_target} PRIVATE
       $<$<OR:$<CXX_COMPILER_ID:Clang>,$<CXX_COMPILER_ID:AppleClang>,$<CXX_COMPILER_ID:GNU>>:
-        -Wall -Wextra -Werror>
+        -Wall -Wextra>
       $<$<CXX_COMPILER_ID:MSVC>:/W0 /D_WIN32_WINNT=0x0A00 /EHsc>
     )
 
@@ -422,7 +422,7 @@
       TritonClient::httpclient ALIAS httpclient
   )
 
-  if (NOT WIN32)
+  if (NOT WIN32 AND NOT APPLE)
      set_target_properties(
        httpclient
        PROPERTIES
@@ -434,9 +434,16 @@
   target_link_libraries(
       httpclient
       PRIVATE triton-common-json
       PUBLIC CURL::libcurl
       PUBLIC Threads::Threads
   )
 
+  if(TRITON_ENABLE_ZLIB)
+    target_link_libraries(
+      httpclient
+      PRIVATE ZLIB::ZLIB
+    )
+  endif()
+
   foreach(_client_target http-client-library httpclient_static httpclient)
     target_compile_features(${_client_target} PRIVATE cxx_std_${TRITON_MIN_CXX_STANDARD})
@@ -443,7 +443,7 @@
     target_compile_options(
       ${_client_target} PRIVATE
       $<$<OR:$<CXX_COMPILER_ID:Clang>,$<CXX_COMPILER_ID:AppleClang>,$<CXX_COMPILER_ID:GNU>>:
-        -Wall -Wextra -Werror>
+        -Wall -Wextra>
       $<$<CXX_COMPILER_ID:MSVC>:/W0 /D_WIN32_WINNT=0x0A00 /EHsc>
     )
 
@@ -463,10 +463,6 @@
         ${CMAKE_CURRENT_SOURCE_DIR}
     )
 
-    target_compile_definitions(
-      ${_client_target}
-        PRIVATE CURL_STATICLIB=1
-    )
     if(TRITON_ENABLE_GPU)
       target_compile_definitions(
         ${_client_target}
@@ -505,12 +501,19 @@
 
   include(GNUInstallDirs)
   set(INSTALL_CONFIGDIR ${CMAKE_INSTALL_LIBDIR}/cmake/TritonClient)
+
+  if(BUILD_SHARED_LIBS)
+    set(TRITON_GRPC_INSTALL_TARGET grpcclient)
+    set(TRITON_HTTP_INSTALL_TARGET httpclient)
+  else()
+    set(TRITON_GRPC_INSTALL_TARGET grpcclient_static)
+    set(TRITON_HTTP_INSTALL_TARGET httpclient_static)
+  endif()
 
   if(TRITON_ENABLE_CC_GRPC)
     install(
       TARGETS
-        grpcclient
-        grpcclient_static
+        ${TRITON_GRPC_INSTALL_TARGET}
       EXPORT
         triton-client-targets
       LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
@@ -522,8 +525,7 @@
   if(TRITON_ENABLE_CC_HTTP)
     install(
       TARGETS
-        httpclient
-        httpclient_static
+        ${TRITON_HTTP_INSTALL_TARGET}
       EXPORT
         triton-client-targets
       LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
diff --git a/src/c++/library/cmake/TritonClientConfig.cmake.in b/src/c++/library/cmake/TritonClientConfig.cmake.in
--- a/src/c++/library/cmake/TritonClientConfig.cmake.in
+++ b/src/c++/library/cmake/TritonClientConfig.cmake.in
@@ -26,6 +26,19 @@
 
 include(CMakeFindDependencyMacro)
 
+find_dependency(Threads)
+find_dependency(TritonCommon CONFIG)
+if(@TRITON_ENABLE_CC_HTTP@)
+  find_dependency(CURL)
+endif()
+if(@TRITON_ENABLE_ZLIB@)
+  find_dependency(ZLIB)
+endif()
+if(@TRITON_ENABLE_CC_GRPC@)
+  find_dependency(Protobuf CONFIG)
+  find_dependency(gRPC CONFIG)
+endif()
+
 get_filename_component(
   TRITONCLIENT_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH
 )
