diff -Naur a/osgplugin/CMakeLists.txt b/osgplugin/CMakeLists.txt
--- a/osgplugin/CMakeLists.txt	2022-09-20 20:32:38.436875800 +0800
+++ b/osgplugin/CMakeLists.txt	2022-09-20 20:56:48.175829800 +0800
@@ -1,7 +1,8 @@
 set(target osgdb_citygml)
 
 set_property(GLOBAL PROPERTY FIND_LIBRARY_USE_LIB64_PATHS ON)
-find_package( OpenSceneGraph REQUIRED osgDB osgViewer osgGA osgUtil osgText)
+find_package(OPENSCENEGRAPH NAMES unofficial-osg) 
+set(OPENSCENEGRAPH_LIBRARIES unofficial::osg::osg unofficial::osg::osgDB unofficial::osg::osgViewer unofficial::osg::osgUtil unofficial::osg::osgText)
 
 include_directories(
   ${OPENSCENEGRAPH_INCLUDE_DIRS}
diff -Naur a/sources/CMakeLists.txt b/sources/CMakeLists.txt
--- a/sources/CMakeLists.txt	2022-06-27 16:20:42.000000000 +0800
+++ b/sources/CMakeLists.txt	2022-10-19 07:56:24.146244100 +0800
@@ -7,7 +7,10 @@
 SET( target citygml )
 
 FIND_PACKAGE( OpenGL REQUIRED )
-FIND_PACKAGE( Xerces REQUIRED )
+FIND_PACKAGE( XercesC REQUIRED )
+set(XERCES_FOUND "${XercesC_FOUND}")
+set(XERCESC_LIBRARY XercesC::XercesC)
+set(XERCESC_LIBRARIES XercesC::XercesC)
 
 # gdal library
 OPTION(LIBCITYGML_USE_GDAL "Set to ON to build libcitygml with GDAL library so that it supports coordinates transformations." ON)
@@ -205,9 +205,9 @@
                        EXPORT_MACRO_NAME LIBCITYGML_EXPORT
                        EXPORT_FILE_NAME ${EXPORT_HEADER_FILE_NAME})
 
 TARGET_LINK_LIBRARIES( ${target} PUBLIC ${XERCESC_LIBRARIES} ${OPENGL_LIBRARIES} )
 if(LIBCITYGML_USE_GDAL)
-    TARGET_LINK_LIBRARIES( ${target} PUBLIC ${GDAL_LIBRARY} )
+    TARGET_LINK_LIBRARIES( ${target} PUBLIC GDAL::GDAL )
 endif(LIBCITYGML_USE_GDAL)
 
 set_target_properties( ${target} PROPERTIES
diff -Naur a/test/CMakeLists.txt b/test/CMakeLists.txt
--- a/test/CMakeLists.txt	2022-06-27 16:20:42.000000000 +0800
+++ b/test/CMakeLists.txt	2022-10-19 08:09:45.011996000 +0800
@@ -3,7 +3,10 @@
 ENDIF()
 
 FIND_PACKAGE( OpenGL REQUIRED )
-FIND_PACKAGE( Xerces REQUIRED )
+FIND_PACKAGE( XercesC REQUIRED )
+set(XERCES_FOUND "${XercesC_FOUND}")
+set(XERCESC_LIBRARY XercesC::XercesC)
+set(XERCESC_LIBRARIES XercesC::XercesC)
 
 IF( LIBCITYGML_DYNAMIC )
   ADD_DEFINITIONS( -DLIBCITYGML_DYNAMIC )
