The package tree-sitter-python can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_python REQUIRED IMPORTED_TARGET tree-sitter-python)

    target_link_libraries(main PRIVATE PkgConfig::ts_python)

