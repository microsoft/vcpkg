The package tree-sitter-cpp can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_cpp REQUIRED IMPORTED_TARGET tree-sitter-cpp)

    target_link_libraries(main PRIVATE PkgConfig::ts_cpp)

