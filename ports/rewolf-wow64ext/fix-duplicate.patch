diff --git a/src/wow64ext.cpp b/src/wow64ext.cpp
index fbba809..ad34ca1 100644
--- a/src/wow64ext.cpp
+++ b/src/wow64ext.cpp
@@ -21,6 +21,9 @@
  */
 
 #include <Windows.h>
+#include <stdlib.h> // Acquire free
+#include <malloc.h> // and malloc
+#include <wchar.h> // Acquire _wcsicmp
 #include <cstddef>
 #include "internal.h"
 #include "wow64ext.h"
@@ -29,6 +32,7 @@
 HANDLE g_heap;
 BOOL g_isWow64;
 
+/*
 void* malloc(size_t size)
 {
 	return HeapAlloc(g_heap, 0, size);
@@ -59,6 +63,7 @@ int _wcsicmp(const wchar_t *string1, const wchar_t *string2)
 	} while (c1 && c1 == c2);
 	return c1 - c2;
 }
+*/
 
 BOOL WINAPI DllMain(HINSTANCE hinstDLL, DWORD fdwReason, LPVOID lpvReserved)
 {
