diff --git a/src/wow64ext.cpp b/src/wow64ext.cpp
index fbba809..6f3d566 100644
--- a/src/wow64ext.cpp
+++ b/src/wow64ext.cpp
@@ -29,6 +29,7 @@
 HANDLE g_heap;
 BOOL g_isWow64;
 
+/*
 void* malloc(size_t size)
 {
 	return HeapAlloc(g_heap, 0, size);
@@ -59,6 +60,7 @@ int _wcsicmp(const wchar_t *string1, const wchar_t *string2)
 	} while (c1 && c1 == c2);
 	return c1 - c2;
 }
+*/
 
 BOOL WINAPI DllMain(HINSTANCE hinstDLL, DWORD fdwReason, LPVOID lpvReserved)
 {
