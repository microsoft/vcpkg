diff --git a/MYSQL_VERSION b/MYSQL_VERSION.txt
similarity index 100%
rename from MYSQL_VERSION
rename to MYSQL_VERSION.txt
diff --git a/cmake/mysql_version.cmake b/cmake/mysql_version.cmake
index 9af34640489..ee9374473a9 100644
--- a/cmake/mysql_version.cmake
+++ b/cmake/mysql_version.cmake
@@ -30,7 +30,7 @@ SET(PROTOCOL_VERSION "10")
 
 # Generate "something" to trigger cmake rerun when MYSQL_VERSION changes
 CONFIGURE_FILE(
-  ${CMAKE_SOURCE_DIR}/MYSQL_VERSION
+  ${CMAKE_SOURCE_DIR}/MYSQL_VERSION.txt
   ${CMAKE_BINARY_DIR}/VERSION.dep
 )
 
@@ -38,7 +38,7 @@ CONFIGURE_FILE(
 
 MACRO(MYSQL_GET_CONFIG_VALUE keyword var)
  IF(NOT ${var})
-   FILE (STRINGS ${CMAKE_SOURCE_DIR}/MYSQL_VERSION str REGEX "^[ ]*${keyword}=")
+   FILE (STRINGS ${CMAKE_SOURCE_DIR}/MYSQL_VERSION.txt str REGEX "^[ ]*${keyword}=")
    IF(str)
      STRING(REPLACE "${keyword}=" "" str ${str})
      STRING(REGEX REPLACE  "[ ].*" ""  str "${str}")
