The package tree-sitter-css can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_css REQUIRED IMPORTED_TARGET tree-sitter-css)

    target_link_libraries(main PRIVATE PkgConfig::ts_css)

