diff --git a/CMakeLists.txt b/CMakeLists.txt
index 75dea11..79513e7 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -14,7 +14,7 @@ if(MSVC)
 endif()

 #---- Include boost to add coroutines ----
-find_package(Boost COMPONENTS coroutine QUIET)
+find_package(Boost REQUIRED) # coroutine2 is header-only
 if(Boost_FOUND)
     include_directories(${Boost_INCLUDE_DIRS})
     string(REPLACE "." "0" Boost_VERSION_NODOT ${Boost_VERSION})
@@ -45,7 +45,7 @@ option(BUILD_SHARED_LIBS "Build shared libraries" ON)

 #---- Find other packages ----
 find_package(Threads)
-find_package(ZMQ)
+find_package(ZMQ REQUIRED)

 list(APPEND BEHAVIOR_TREE_EXTERNAL_LIBRARIES
     ${CMAKE_THREAD_LIBS_INIT}
