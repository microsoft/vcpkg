diff --git a/gir/meson.build b/gir/meson.build
index 3a01683..d7ab7eb 100644
--- a/gir/meson.build
+++ b/gir/meson.build
@@ -511,6 +511,9 @@ if get_option('gi_cross_binary_wrapper') != ''
 else
   gircompiler_command = [gircompiler, ]
 endif
+if get_option('gi_cross_use_prebuilt_gi')
+  gircompiler = [] # do not use as a dependency
+endif
 
 gircompiler_command += [
   '-o', '@OUTPUT@',
diff --git a/giscanner/meson.build b/giscanner/meson.build
index ae370ce..967d651 100644
--- a/giscanner/meson.build
+++ b/giscanner/meson.build
@@ -1,3 +1,9 @@
+if get_option('gi_cross_use_prebuilt_gi')
+  gdump = []
+  giscanner_pymod = []
+  subdir_done()
+endif
+
 giscanner_files = [
   '__init__.py',
   'annotationmain.py',
diff --git a/meson.build b/meson.build
index b1597cc..6812c92 100644
--- a/meson.build
+++ b/meson.build
@@ -175,7 +175,9 @@ configure_file(
 )
 
 # python headers
+if not get_option('gi_cross_use_prebuilt_gi')
 cc.check_header('Python.h', dependencies: [python.dependency()], required: true)
+endif
 
 # cairo
 cairo_option = get_option('cairo')
diff --git a/tools/meson.build b/tools/meson.build
index f83781e..2da7d25 100644
--- a/tools/meson.build
+++ b/tools/meson.build
@@ -57,6 +57,10 @@ if cc.get_id() != 'msvc'
   custom_c_args = cc.get_supported_arguments(['-Wno-missing-field-initializers'])
 endif
 
+if get_option('gi_cross_use_prebuilt_gi')
+  gircompiler = find_program('g-ir-compiler')
+  subdir_done()
+endif
 
 gircompiler = executable('g-ir-compiler', 'compiler.c',
   dependencies: [
