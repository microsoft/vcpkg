diff --git a/include/X11/XKBlib.h b/include/X11/XKBlib.h
index 8f6c72c12..3b4b61236 100644
--- a/include/X11/XKBlib.h	
+++ b/include/X11/XKBlib.h
@@ -29,7 +29,9 @@ THE USE OR PERFORMANCE OF THIS SOFTWARE.
 
 #include <X11/Xlib.h>
 #include <X11/extensions/XKBstr.h>
-
+#if !defined(Status)
+	typedef int Status;
+#endif
 typedef struct _XkbAnyEvent {
 	int 		type;		/* XkbAnyEvent */
 	unsigned long 	serial;		/* # of last req processed by server */
diff --git a/include/X11/Xlibint.h b/include/X11/Xlibint.h
index 844571688..e46c4ec5d 100644
--- a/include/X11/Xlibint.h	
+++ b/include/X11/Xlibint.h
@@ -1351,11 +1351,19 @@ extern int _XOpenFile(
     int			/* flags */
 );
 
-extern int _XOpenFileMode(
+#ifdef _MSC_VER
+    extern int _XOpenFileMode(
+    _Xconst char*	/* path */,
+    int			/* flags */,
+    int              /* mode */
+);
+#else
+    extern int _XOpenFileMode(
     _Xconst char*	/* path */,
     int			/* flags */,
     mode_t              /* mode */
 );
+#endif
 
 extern void* _XFopenFile(
     _Xconst char*	/* path */,
diff --git a/include/X11/Xresource.h b/include/X11/Xresource.h
index 6dbb3ce46..87aac8813 100644
--- a/include/X11/Xresource.h	
+++ b/include/X11/Xresource.h
@@ -267,7 +267,7 @@ extern XrmDatabase XrmGetFileDatabase(
     _Xconst char*	/* filename */
 );
 
-extern Status XrmCombineFileDatabase(
+extern int XrmCombineFileDatabase(
     _Xconst char* 	/* filename */,
     XrmDatabase*	/* target */,
     Bool		/* override */
diff --git a/modules/im/ximcp/imLcLkup.c b/modules/im/ximcp/imLcLkup.c
index 56dba9673..d10de825e 100644
--- a/modules/im/ximcp/imLcLkup.c	
+++ b/modules/im/ximcp/imLcLkup.c
@@ -61,27 +61,37 @@ _XimLocalMbLookupString(XIC xic, XKeyEvent *ev, char *buffer, int bytes,
 	    ||(ic->private.local.brl_committed != 0))) {
 	if (ic->private.local.brl_committed != 0) { /* Braille Event */
 	    unsigned char pattern = ic->private.local.brl_committed;
+#ifndef _MSC_VER
 	    char mb2[XLC_PUBLIC(ic->core.im->core.lcd, mb_cur_max)];
+#else
+        char *mb2 = (char*)malloc(XLC_PUBLIC(ic->core.im->core.lcd, mb_cur_max)*sizeof(char));
+#endif
 	    ret = _Xlcwctomb(ic->core.im->core.lcd, mb2, BRL_UC_ROW | pattern);
 	    if(ret > bytes) {
-		if(status) *status = XBufferOverflow;
-		return(ret);
+            if(status) *status = XBufferOverflow;
+#ifdef _MSC_VER
+            free(mb2);
+#endif
+            return(ret);
 	    }
 	    if(keysym) *keysym = XK_braille_blank | pattern;
 	    if(ret > 0) {
-		if (keysym) {
-		    if(status) *status = XLookupBoth;
-		} else {
-		    if(status) *status = XLookupChars;
+            if (keysym) {
+                if(status) *status = XLookupBoth;
+            } else {
+                if(status) *status = XLookupChars;
 		}
 		memcpy(buffer, mb2, ret);
 	    } else {
-		if(keysym) {
-		    if(status) *status = XLookupKeySym;
-		} else {
-		    if(status) *status = XLookupNone;
-		}
+            if(keysym) {
+                if(status) *status = XLookupKeySym;
+            } else {
+                if(status) *status = XLookupNone;
+            }
 	    }
+#ifdef _MSC_VER
+        free(mb2);
+#endif
 	} else { /* Composed Event */
 	    ret = strlen(&mb[b[ic->private.local.composed].mb]);
 	    if(ret > bytes) {
diff --git a/src/ClDisplay.c b/src/ClDisplay.c
index aa904e516..dafb76a6c 100644
--- a/src/ClDisplay.c	
+++ b/src/ClDisplay.c
@@ -55,7 +55,7 @@ XCloseDisplay (
 		    register Screen *sp = &dpy->screens[i];
 		    XFreeGC (dpy, sp->default_gc);
 	    }
-	    if (dpy->cursor_font != None) {
+	    if (dpy->cursor_font != 0) {
 		XUnloadFont (dpy, dpy->cursor_font);
 	    }
 	    XSync(dpy, 1);  /* throw away pending events, catch errors */
diff --git a/src/OpenDis.c b/src/OpenDis.c
index 717de3390..b1982449a 100644
--- a/src/OpenDis.c	
+++ b/src/OpenDis.c
@@ -160,7 +160,7 @@ XOpenDisplay (
 	}
 	dpy->resource_id	= 0;
 	dpy->db 		= (struct _XrmHashBucketRec *)NULL;
-	dpy->cursor_font	= None;
+	dpy->cursor_font	= 0;
 	dpy->flags		= 0;
 	dpy->async_handlers	= NULL;
 	dpy->screens		= NULL;
@@ -545,7 +545,7 @@ XOpenDisplay (
 		    _XReadPad (dpy, dpy->xdefaults, reply.nItems);
 		    dpy->xdefaults[reply.nItems] = '\0';
 		}
-		else if (reply.propertyType != None)
+		else if (reply.propertyType != 0)
 		    _XEatDataWords(dpy, reply.length);
 	    }
 	}
diff --git a/src/XlibInt.c b/src/XlibInt.c
index e4fb4e5f2..4cfa1aeab 100644
--- a/src/XlibInt.c	
+++ b/src/XlibInt.c
@@ -60,6 +60,8 @@ from The Open Group.
 /* Needed for ioctl() on Solaris */
 #ifdef HAVE_UNISTD_H
 #include <unistd.h>
+#else
+    #define F_OK 0
 #endif
 
 #ifdef XTHREADS
@@ -985,7 +987,7 @@ _XWireToEvent(
 	      case KeymapNotify:
 		{
 			register XKeymapEvent *ev = (XKeymapEvent *) re;
-			ev->window	= None;
+			ev->window	= 0;
 			memcpy(&ev->key_vector[1],
 			       (char *)((xKeymapEvent *) event)->map,
 			       sizeof (((xKeymapEvent *) event)->map));
@@ -1911,7 +1913,11 @@ int _XOpenFile(path, flags)
 int _XOpenFileMode(path, flags, mode)
     _Xconst char* path;
     int flags;
+#ifndef _MSC_VER
     mode_t mode;
+#else
+    int mode;
+#endif
 {
     char buf[MAX_PATH];
     char* bufp = NULL;
@@ -1919,7 +1925,11 @@ int _XOpenFileMode(path, flags, mode)
     UINT olderror = SetErrorMode (SEM_FAILCRITICALERRORS);
 
     if (AccessFile (path, buf, MAX_PATH, &bufp))
+#ifndef _MSC_VER
 	ret = open (bufp, flags, mode);
+#else
+    ret = _open (bufp, flags, mode);
+#endif
 
     (void) SetErrorMode (olderror);
 
diff --git a/src/locking.c b/src/locking.c
index 9f4fe0677..2ba426627 100644
--- a/src/locking.c	
+++ b/src/locking.c
@@ -569,7 +569,7 @@ xthread_t (*_x11_thr_self)() = __x11_thr_self;
 #endif
 
 
-Status XInitThreads(void)
+int XInitThreads(void)
 {
     if (_Xglobal_lock)
 	return 1;
@@ -618,7 +618,7 @@ Status XInitThreads(void)
 }
 
 #else /* XTHREADS */
-Status XInitThreads(void)
+int XInitThreads(void)
 {
     return 0;
 }
diff --git a/src/xlibi18n/XDefaultOMIF.c b/src/xlibi18n/XDefaultOMIF.c
index 9345547c3..77d914fe1 100644
--- a/src/xlibi18n/XDefaultOMIF.c	
+++ b/src/xlibi18n/XDefaultOMIF.c
@@ -70,6 +70,14 @@ Sun Microsystems, Inc. or its licensors is granted.
 #include <X11/Xatom.h>
 #include <stdio.h>
 
+#if !defined(ssize_t)
+    #ifdef _WIN64
+    #define ssize_t long long
+    #else
+    #define ssize_t long
+    #endif
+#endif
+
 #define MAXFONTS		100
 
 #define XOM_GENERIC(om)		(&((XOMGeneric) om)->gen)
diff --git a/src/xlibi18n/lcDB.c b/src/xlibi18n/lcDB.c
index e04a5d89f..fa46f8ecc 100644
--- a/src/xlibi18n/lcDB.c	
+++ b/src/xlibi18n/lcDB.c
@@ -65,6 +65,14 @@
 
 #include <stdio.h>
 
+#if !defined(ssize_t)
+    #ifdef _WIN64
+    #define ssize_t long long
+    #else
+    #define ssize_t long
+    #endif
+#endif
+
 typedef struct _DatabaseRec {
     char *category;
     char *name;
diff --git a/src/xlibi18n/lcFile.c b/src/xlibi18n/lcFile.c
index e43ac2c0e..d9f49b725 100644
--- a/src/xlibi18n/lcFile.c	
+++ b/src/xlibi18n/lcFile.c
@@ -32,7 +32,10 @@
 #include "Xlibint.h"
 #include "XlcPubI.h"
 #include <X11/Xos.h>
+#ifdef HAVE_UNISTD_H
 #include <unistd.h>
+#else
+#endif
 
 /************************************************************************/
 
