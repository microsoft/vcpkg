The package pthreads provides CMake targets:

    find_package(pthreads REQUIRED)
    target_link_libraries(main PRIVATE PThreads4W::PThreads4W PThreads4W::PThreads4W_CXXEXC PThreads4W::PThreads4W_STRUCTEXC)
    target_include_directories(main PRIVATE PThreads4W_INCLUDE_DIR)