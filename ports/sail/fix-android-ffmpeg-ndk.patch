diff --git a/src/sail-codecs-archive/CMakeLists.txt b/src/sail-codecs-archive/CMakeLists.txt
index 1111111..2222222 100644
--- a/src/sail-codecs-archive/CMakeLists.txt
+++ b/src/sail-codecs-archive/CMakeLists.txt
@@ -81,6 +81,12 @@ if (APPLE AND NOT BUILD_SHARED_LIBS AND "video" IN_LIST ENABLED_CODECS)
     )
 endif()
 
+# For static Android builds, FFmpeg requires additional NDK libraries
+#
+if (ANDROID AND NOT BUILD_SHARED_LIBS AND "video" IN_LIST ENABLED_CODECS)
+    target_link_libraries(sail-codecs INTERFACE mediandk android)
+endif()
+
 target_include_directories(sail-codecs PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../sail)
 
 install(TARGETS sail-codecs
diff --git a/src/sail-manip/CMakeLists.txt b/src/sail-manip/CMakeLists.txt
index 3333333..4444444 100644
--- a/src/sail-manip/CMakeLists.txt
+++ b/src/sail-manip/CMakeLists.txt
@@ -71,6 +71,10 @@ if (SAIL_MANIP_SWSCALE_ENABLED)
     target_include_directories(sail-manip PRIVATE ${SAIL_SWSCALE_INCLUDE_DIR} ${SAIL_AVUTIL_INCLUDE_DIR})
     target_link_libraries(sail-manip PRIVATE ${SAIL_SWSCALE_LIBS} ${SAIL_AVUTIL_LIBS})
     target_compile_definitions(sail-manip PRIVATE SAIL_MANIP_SWSCALE_ENABLED)
+
+    if (ANDROID AND NOT BUILD_SHARED_LIBS)
+        target_link_libraries(sail-manip INTERFACE mediandk android)
+    endif()
 endif()
 
 # pkg-config integration
