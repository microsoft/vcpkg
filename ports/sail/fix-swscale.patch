diff --git a/cmake/sail_find_swscale.cmake b/cmake/sail_find_swscale.cmake
index c4e6804..e6cc866 100644
--- a/cmake/sail_find_swscale.cmake
+++ b/cmake/sail_find_swscale.cmake
@@ -13,9 +13,11 @@ macro(sail_find_swscale MODE)
 
     if (${MODE} STREQUAL "ON" OR ${MODE} STREQUAL "AUTO")
         find_path(SAIL_SWSCALE_INCLUDE_DIR NAMES libswscale/swscale.h)
+        find_path(SAIL_AVUTIL_INCLUDE_DIR NAMES libavutil/pixfmt.h)
         find_library(SAIL_SWSCALE_LIBS NAMES swscale)
+        find_library(SAIL_AVUTIL_LIBS NAMES avutil)
 
-        if (SAIL_SWSCALE_INCLUDE_DIR AND SAIL_SWSCALE_LIBS)
+        if (SAIL_SWSCALE_INCLUDE_DIR AND SAIL_AVUTIL_INCLUDE_DIR AND SAIL_SWSCALE_LIBS AND SAIL_AVUTIL_LIBS)
             message(STATUS "Using libswscale for pixel format conversion in sail-manip")
             set(SAIL_MANIP_SWSCALE_ENABLED ON)
             set(SAIL_MANIP_SWSCALE_ENABLED_DISPLAY "ON")
@@ -23,7 +25,7 @@ macro(sail_find_swscale MODE)
             if (${MODE} STREQUAL "ON")
                 message(FATAL_ERROR "libswscale not found but SAIL_MANIP_USE_SWSCALE=ON requires it")
             else()
-                message(STATUS "libswscale not found, not using swscale for pixel format conversion")
+                message(STATUS "libswscale or libavutil not found, not using swscale for pixel format conversion")
             endif()
         endif()
     elseif (NOT ${MODE} STREQUAL "OFF")
diff --git a/src/sail-manip/CMakeLists.txt b/src/sail-manip/CMakeLists.txt
index d986c44..6b2ea9e 100644
--- a/src/sail-manip/CMakeLists.txt
+++ b/src/sail-manip/CMakeLists.txt
@@ -68,9 +68,13 @@ if (MATH_LIBRARY)
 endif()
 
 if (SAIL_MANIP_SWSCALE_ENABLED)
-    target_include_directories(sail-manip PRIVATE ${SAIL_SWSCALE_INCLUDE_DIR})
-    target_link_libraries(sail-manip PRIVATE ${SAIL_SWSCALE_LIBS})
+    target_include_directories(sail-manip PRIVATE ${SAIL_SWSCALE_INCLUDE_DIR} ${SAIL_AVUTIL_INCLUDE_DIR})
+    target_link_libraries(sail-manip PRIVATE ${SAIL_SWSCALE_LIBS} ${SAIL_AVUTIL_LIBS})
     target_compile_definitions(sail-manip PRIVATE SAIL_MANIP_SWSCALE_ENABLED)
+
+    if (ANDROID AND NOT BUILD_SHARED_LIBS)
+        target_link_libraries(sail-manip INTERFACE mediandk android)
+    endif()
 endif()
 
 # pkg-config integration
diff --git a/src/sail-manip/scale_swscale.c b/src/sail-manip/scale_swscale.c
index 8bf1d5f..398f1e9 100644
--- a/src/sail-manip/scale_swscale.c
+++ b/src/sail-manip/scale_swscale.c
@@ -25,9 +25,6 @@
 
 #include <stdint.h>
 
-#include <libavutil/pixfmt.h>
-#include <libswscale/swscale.h>
-
 #include <sail-common/export.h>
 #include <sail-common/sail-common.h>
 
@@ -35,6 +32,9 @@
 
 #ifdef SAIL_MANIP_SWSCALE_ENABLED
 
+#include <libavutil/pixfmt.h>
+#include <libswscale/swscale.h>
+
 /* Convert SailScaling enum to swscale flags. */
 int sail_scaling_to_swscale_flags(enum SailScaling algorithm)
 {
diff --git a/src/sail-manip/swscale_conversions.c b/src/sail-manip/swscale_conversions.c
index cce37f5..3365f4ac 100644
--- a/src/sail-manip/swscale_conversions.c
+++ b/src/sail-manip/swscale_conversions.c
@@ -26,15 +26,15 @@
 #include <stdbool.h>
 #include <stdint.h>
 
-#include <libavutil/pixfmt.h>
-#include <libswscale/swscale.h>
-
 #include <sail-common/sail-common.h>
 
 #include "swscale_conversions.h"
 
 #ifdef SAIL_MANIP_SWSCALE_ENABLED
 
+#include <libavutil/pixfmt.h>
+#include <libswscale/swscale.h>
+
 /*
  * Private functions.
  */
