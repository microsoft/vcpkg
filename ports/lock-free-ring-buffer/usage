lock-free-ring-buffer is a header-only library.

In exactly one .c/.cpp file, define the implementation before including:

    #define RING_BUFFER_IMPLEMENTATION
    #include <ring_buffer.h>

In all other files, include normally:

    #include <ring_buffer.h>
