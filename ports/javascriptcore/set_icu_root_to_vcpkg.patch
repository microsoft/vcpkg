diff --git a/Source/cmake/OptionsWin.cmake b/Source/cmake/OptionsWin.cmake
index 1d8bd1afeb47..3dc641534318 100644
--- a/Source/cmake/OptionsWin.cmake
+++ b/Source/cmake/OptionsWin.cmake
@@ -76,7 +76,18 @@ set(CMAKE_DISABLE_PRECOMPILE_HEADERS OFF)
 
 find_package(Cairo 1.18.0 REQUIRED)
 find_package(CURL 7.87.0 REQUIRED)
+if(CMAKE_BUILD_TYPE STREQUAL "Debug")
+    set(ICU_ROOT "${CMAKE_BINARY_DIR}/vcpkg_installed/x64-windows/debug")
+    set(ICU_INCLUDE_DIR "${CMAKE_BINARY_DIR}/vcpkg_installed/x64-windows/include")
+else()
+    set(ICU_ROOT "${CMAKE_BINARY_DIR}/vcpkg_installed/x64-windows")
+endif()
 find_package(ICU 61.2 REQUIRED COMPONENTS data i18n uc)
+if(ICU_FOUND)
+    message(">> ICU_FOUND Version: ${ICU_VERSION}")
+else()
+    message(FATAL_ERROR ">> ICU not found")
+endif()
 find_package(JPEG 1.5.2 REQUIRED)
 find_package(LibXml2 2.9.7 REQUIRED)
 find_package(OpenSSL REQUIRED)
