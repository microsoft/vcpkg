The package tree-sitter-typescript can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_typescript REQUIRED IMPORTED_TARGET tree-sitter-typescript)

    target_link_libraries(main PRIVATE PkgConfig::ts_typescript)

