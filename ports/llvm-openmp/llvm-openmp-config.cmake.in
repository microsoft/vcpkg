@PACKAGE_INIT@

include(${CMAKE_CURRENT_LIST_DIR}/unofficial-llvm-openmp-targets.cmake)

if(WIN32)
    set_target_properties(omp PROPERTIES
        IMPORTED_IMPLIB ${CMAKE_CURRENT_LIST_FILE}/../../../lib/libomp.lib
    )
endif()
