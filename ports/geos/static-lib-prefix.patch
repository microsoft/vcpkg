diff --git a/CMakeLists.txt b/CMakeLists.txt
index b78ca54b..26778c83 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -228,6 +228,10 @@ if(BUILD_SHARED_LIBS)
 
   set_target_properties(geos PROPERTIES VERSION ${GEOS_VERSION})
   set_target_properties(geos PROPERTIES SOVERSION ${GEOS_VERSION})
+else()
+  if(WIN32)
+    set_target_properties(geos PROPERTIES PREFIX lib)
+  endif()
 endif()
 
 #-----------------------------------------------------------------------------
@@ -244,6 +248,10 @@ if(BUILD_SHARED_LIBS)
   if(NOT WIN32)
     set_target_properties(geos_c PROPERTIES SOVERSION ${CAPI_VERSION_MAJOR})
   endif()
+else()
+  if(WIN32)
+    set_target_properties(geos_c PROPERTIES PREFIX lib)
+  endif()
 endif()
 
 add_subdirectory(capi)
