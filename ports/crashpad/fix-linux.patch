diff --git a/util/misc/uuid.cc b/util/misc/uuid.cc
index 3013d7b..3a575e5 100644
--- a/util/misc/uuid.cc
+++ b/util/misc/uuid.cc
@@ -41,7 +41,7 @@
 namespace crashpad {
 
 static_assert(sizeof(UUID) == 16, "UUID must be 16 bytes");
-static_assert(std::is_pod<UUID>::value, "UUID must be POD");
+static_assert(std::is_trivial<UUID>::value && std::is_standard_layout<UUID>::value, "UUID must be trivial and standard layout");
 
 bool UUID::operator==(const UUID& that) const {
   return memcmp(this, &that, sizeof(*this)) == 0;
