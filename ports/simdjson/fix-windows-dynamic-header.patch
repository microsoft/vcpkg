diff --git a/CMakeLists.txt b/CMakeLists.txt
index 29d2a47..57d68f9 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -233,8 +233,24 @@ if(SIMDJSON_INSTALL)
     include(GNUInstallDirs)
 
     if(SIMDJSON_SINGLEHEADER)
+      set(SIMDJSON_INSTALL_HEADER "${PROJECT_SOURCE_DIR}/singleheader/simdjson.h")
+      if(WIN32 AND BUILD_SHARED_LIBS)
+        set(SIMDJSON_INSTALL_HEADER "${PROJECT_BINARY_DIR}/simdjson.h")
+        configure_file(
+            "${PROJECT_SOURCE_DIR}/singleheader/simdjson.h"
+            "${SIMDJSON_INSTALL_HEADER}"
+            COPYONLY
+        )
+        file(READ "${SIMDJSON_INSTALL_HEADER}" SIMDJSON_INSTALL_HEADER_CONTENTS)
+        file(WRITE "${SIMDJSON_INSTALL_HEADER}"
+            "#ifndef SIMDJSON_USING_WINDOWS_DYNAMIC_LIBRARY\n"
+            "#define SIMDJSON_USING_WINDOWS_DYNAMIC_LIBRARY 1\n"
+            "#endif\n"
+            "${SIMDJSON_INSTALL_HEADER_CONTENTS}"
+        )
+      endif()
       install(
-          FILES singleheader/simdjson.h
+          FILES "${SIMDJSON_INSTALL_HEADER}"
           DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}"
           COMPONENT simdjson_Development
       )
