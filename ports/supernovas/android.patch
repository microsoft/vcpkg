diff --git a/src/timescale.c b/src/timescale.c
index 50edfe2f..de8aefe0 100644
--- a/src/timescale.c
+++ b/src/timescale.c
@@ -891,7 +891,7 @@ int novas_set_unix_time(time_t unix_time, long nanos, int leap, double dut1, nov
 int novas_set_current_time(int leap, double dut1, novas_timespec *restrict time) {
   struct timespec t = {};
 
-#if __STDC_VERSION__ >= 201112L || defined(_MSC_VER)
+#if (__STDC_VERSION__ >= 201112L && !defined(__ANDROID__)) || defined(_MSC_VER)
   timespec_get(&t, TIME_UTC);
 #else
   clock_gettime(CLOCK_REALTIME, &t);
diff --git a/test/src/test-super.c b/test/src/test-super.c
index db6b6eaf..f411936f 100644
--- a/test/src/test-super.c
+++ b/test/src/test-super.c
@@ -2140,7 +2140,7 @@ static int test_set_current_time() {
   struct timespec now = {};
   novas_timespec t1 = {}, t2 = {};
 
-#if __STDC_VERSION__ >= 201112L || defined(_MSC_VER)
+#if (__STDC_VERSION__ >= 201112L && !defined(__ANDROID__)) || defined(_MSC_VER)
   timespec_get(&now, TIME_UTC);
 #else
   clock_gettime(CLOCK_REALTIME, &now);
