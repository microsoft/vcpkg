diff --git a/ctx.h b/ctx.h
index 89cda84..1b8a774 100644
--- a/ctx.h
+++ b/ctx.h
@@ -55,6 +55,27 @@ extern "C" {
 #include <alloca.h>
 #else
 #include <malloc.h>
+#ifdef _MSC_VER
+#include <io.h>
+#ifndef access
+#define access _access
+#endif
+#ifndef F_OK
+#define F_OK 0
+#endif
+#ifndef R_OK
+#define R_OK 4
+#endif
+#ifndef strcasecmp
+#define strcasecmp _stricmp
+#endif
+#ifndef strncasecmp
+#define strncasecmp _strnicmp
+#endif
+#ifndef S_ISDIR
+#define S_ISDIR(mode) (((mode) & _S_IFMT) == _S_IFDIR)
+#endif
+#endif
 #endif
 #include <stdio.h>
 
@@ -4150,8 +4171,12 @@ extern float ctx_target_fps;
 
 
 #ifndef CTX_EVENTS
+#ifdef _WIN32
+#define CTX_EVENTS              0
+#else
 #define CTX_EVENTS              1
 #endif
+#endif
 
 #ifndef CTX_MAX_DEVICES
 #define CTX_MAX_DEVICES 16
@@ -4467,9 +4492,7 @@ extern float ctx_target_fps;
 #endif
 
 #if CTX_SDL || CTX_FB || CTX_HEADLESS
-#if CTX_EVENTS
 #undef CTX_EVENTS
-#endif
 #define CTX_EVENTS 1
 #endif
 
@@ -22979,7 +23002,7 @@ CTX_SIMD_SUFFIX (ctx_composite_fill_rect) (CtxRasterizer *rasterizer,
   if ((width >0) & (height>0))
   {
      uint8_t *dst = ( (uint8_t *) rasterizer->buf);
-     uint8_t coverage[width+2];
+     uint8_t *coverage = (uint8_t *) alloca (sizeof (uint8_t) * (size_t) (width + 2));
      uint32_t x0i = (int)x0+has_left;
      uint32_t x1i = (int)x1-has_right;
      uint32_t y0i = (int)y0+has_top;
diff --git a/ctx_config.h b/ctx_config.h
index 999e826..fd9a143 100644
--- a/ctx_config.h
+++ b/ctx_config.h
@@ -4,7 +4,13 @@
 #include "local.conf"
 
 #define CTX_MIPMAP               1
+#ifdef _WIN32
+#ifndef CTX_SHARE
+#define CTX_SHARE                CTX_SOCKETS
+#endif
+#else
 #define CTX_SHARE                1
+#endif
 
 
 #define CTX_CURRENT_PATH         1
@@ -45,7 +51,9 @@
 #define CTX_GET_CONTENTS         1
 #define CTX_MAGIC                1
 #define CTX_PROTOCOL_U8_COLOR    1
+#ifndef CTX_NET
 #define CTX_NET                  1
+#endif
 
 #ifdef CTX_WANT_FONT
 
