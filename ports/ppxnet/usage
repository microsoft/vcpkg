The package ppxnet is compatible with built-in CMake targets:

    find_package(ppxnet REQUIRED)
    target_link_libraries(main PRIVATE ppxnet)
or:
    target_link_libraries(main PRIVATE ppxnet-static)
