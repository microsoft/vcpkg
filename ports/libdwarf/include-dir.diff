diff --git a/src/lib/libdwarf/CMakeLists.txt b/src/lib/libdwarf/CMakeLists.txt
index 30048ee..a64badc 100644
--- a/src/lib/libdwarf/CMakeLists.txt
+++ b/src/lib/libdwarf/CMakeLists.txt
@@ -140,7 +140,7 @@ target_link_libraries(dwarf-shared PRIVATE $<$<AND:$<BOOL:${ZLIB_FOUND}>,$<BOOL:
 target_include_directories(dwarf-shared PUBLIC
     $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/src/lib/libdwarf>
     $<BUILD_INTERFACE:${PROJECT_BINARY_DIR}>
-    $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>
+    $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}/libdwarf>
   )
 add_library(libdwarf::dwarf ALIAS dwarf-shared)
 
@@ -161,7 +161,7 @@ target_link_libraries(dwarf-static PRIVATE $<$<AND:$<BOOL:${ZLIB_FOUND}>,$<BOOL:
 target_include_directories(dwarf-static PUBLIC
     $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/src/lib/libdwarf>
     $<BUILD_INTERFACE:${PROJECT_BINARY_DIR}>
-    $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>
+    $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}/libdwarf>
   )
 add_library(libdwarf::dwarf-static ALIAS dwarf-static)
 
@@ -177,20 +177,25 @@ write_basic_package_version_file("${CMAKE_CURRENT_BINARY_DIR}/libdwarfConfigVers
 install(FILES "${CMAKE_CURRENT_BINARY_DIR}/libdwarfConfig.cmake" "${CMAKE_CURRENT_BINARY_DIR}/libdwarfConfigVersion.cmake" DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/libdwarf")
 
 if(INSTALL_STATIC_LIBRARIES)
-  set(DWARF_STATIC_LIB "dwarf-static")
+  set(DWARF_INSTALL_TARGET dwarf-static)
+  set_target_properties(dwarf-static PROPERTIES OUTPUT_NAME dwarf EXPORT_NAME dwarf)
+  set_target_properties(dwarf-shared PROPERTIES ARCHIVE_OUTPUT_NAME dwarf-shared)
+else()
+  set(DWARF_INSTALL_TARGET dwarf-shared)
 endif()
 
-install(TARGETS dwarf-shared ${DWARF_STATIC_LIB}
+install(TARGETS ${DWARF_INSTALL_TARGET}
         EXPORT libdwarfTargets
         ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}"
         LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
-        PUBLIC_HEADER DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}")
+        RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
+        PUBLIC_HEADER DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/libdwarf")
 install(EXPORT libdwarfTargets
         FILE libdwarf-targets.cmake
         NAMESPACE libdwarf::
         DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/libdwarf")
 export(
-  TARGETS dwarf-shared ${DWARF_STATIC_LIB}
+  TARGETS ${DWARF_INSTALL_TARGET}
   NAMESPACE libdwarf::
   FILE "${CMAKE_CURRENT_BINARY_DIR}/libdwarf-targets.cmake"
 )
diff --git a/src/lib/libdwarf/libdwarf.pc.in b/src/lib/libdwarf/libdwarf.pc.in
index 3bb9178..4a094d1 100644
--- a/src/lib/libdwarf/libdwarf.pc.in
+++ b/src/lib/libdwarf/libdwarf.pc.in
@@ -2,7 +2,7 @@
 prefix=@CMAKE_INSTALL_PREFIX@
 exec_prefix="${prefix}"
 libdir="${prefix}/lib"
-includedir="${prefix}/include"
+includedir="${prefix}/include/libdwarf"
 
 Name: libdwarf
 Description: DWARF debug symbols library
