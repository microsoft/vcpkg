In case of multiple OpenCV version installed, set the OpenCV_ROOT *before* the find_package call

    set(OpenCV_ROOT "${VCPKG_INSTALLED_DIR}/@TARGET_TRIPLET@/share/@PORT@")
    find_package(OpenCV REQUIRED)
