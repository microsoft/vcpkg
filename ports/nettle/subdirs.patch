diff --git a/Makefile.in b/Makefile.in
index 9399985..165e56f 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -20,7 +20,7 @@ OPT_NETTLE_SOURCES = @OPT_NETTLE_SOURCES@
 FAT_OVERRIDE_LIST = @FAT_OVERRIDE_LIST@
 FAT_EMULATE_LIST = @FAT_EMULATE_LIST@
 
-SUBDIRS = testsuite tools examples
+SUBDIRS = @IF_TOOLS@ tools
 
 include config.make
 
diff --git a/configure.ac b/configure.ac
index af017d8..8a33ac1 100644
--- a/configure.ac
+++ b/configure.ac
@@ -24,6 +24,14 @@ AC_SUBST([MINOR_VERSION])
 
 AC_CANONICAL_HOST
 
+AC_ARG_ENABLE(tools,[])
+if test "x$enable_tools" = xyes ; then
+  IF_TOOLS=''
+else
+  IF_TOOLS='#'
+fi
+AC_SUBST([IF_TOOLS])
+
 # Command line options
 AC_ARG_ENABLE(public-key,
   AS_HELP_STRING([--disable-public-key], [Disable public key algorithms]),,
