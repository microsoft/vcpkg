The package tree-sitter-yaml can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_yaml REQUIRED IMPORTED_TARGET tree-sitter-yaml)

    target_link_libraries(main PRIVATE PkgConfig::ts_yaml)

