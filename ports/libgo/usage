The package libgo is compatible with built-in CMake targets:

    find_package(libgo REQUIRED)
    target_include_directories(main PRIVATE ${LIBGO_INCLUDE_DIRS})
    target_link_libraries(main PRIVATE ${LIBGO_LIBRARIES} -static)
