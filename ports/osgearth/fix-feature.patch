diff --git a/CMakeLists.txt b/CMakeLists.txt
index 22f0207..28946f4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -102,10 +102,8 @@ ELSE ()
 ENDIF (OSGEARTH_USE_GLES)
 
 FIND_PACKAGE(CURL)
-FIND_PACKAGE(GDAL)
 FIND_PACKAGE(GEOS)
 FIND_PACKAGE(Sqlite3)
-FIND_PACKAGE(Draco)
 FIND_PACKAGE(BASISU)
 FIND_PACKAGE(Tracy)
 FIND_PACKAGE(GLEW)
@@ -115,10 +113,21 @@ IF(SQLITE3_FOUND)
     ADD_DEFINITIONS(-DOSGEARTH_HAVE_MBTILES)
 ENDIF(SQLITE3_FOUND)
 
+if(BUILD_WITH_DRACO)
+  find_path(DRACO_INCLUDE_DIRS draco/core/draco_version.h)
+  find_library(DRACO_LIBRARY NAMES draco dracodec dracoenc libdraco libdracodec libdracoenc)
+  set(draco_INCLUDE_DIRS ${DRACO_INCLUDE_DIRS})
+  set(draco_LIBRARIES ${DRACO_LIBRARY})
+  if(DRACO_LIBRARY)
+    set(draco_FOUND 1)
+  endif()
 IF(draco_FOUND)
     ADD_DEFINITIONS(-DOSGEARTH_HAVE_DRACO)
 ENDIF(draco_FOUND)
+endif()
 
+if(BUILD_WITH_GDAL)
+  FIND_PACKAGE(GDAL REQUIRED)
 IF(GDAL_FOUND)
   IF (GDAL_VERSION VERSION_LESS 3)
     message(STATUS "Found GDAL ${GDAL_VERSION}" )
@@ -126,6 +135,7 @@ IF(GDAL_FOUND)
     message(FATAL_ERROR "GDAL ${GDAL_VERSION} is unsupported at this time. Please use GDAL 2.X" )
   ENDIF()
 ENDIF(GDAL_FOUND)
+endif()
 
 OPTION(ENABLE_PROFILING "Build with support for Tracy profiler" OFF)
 IF(TRACY_FOUND AND ENABLE_PROFILING)
