--- a/SRC/CMakeLists.txt
+++ b/SRC/CMakeLists.txt
@@ -361,7 +361,7 @@
 endif()
 
 set(ALLOBJ ${SLASRC} ${DLASRC} ${CLASRC} ${ZLASRC} ${SCLAUX} ${DZLAUX}	
-	${ALLAUX})
+	${ALLAUX} $<TARGET_OBJECTS:f2c>)
 if(BUILD_SINGLE)
 set(ALLOBJ  ${SLASRC} ${ALLAUX} 
 	${SCLAUX})
@@ -382,7 +382,7 @@
 endif(BUILD_SHARED_LIBS AND MSVC)
 
 
-target_link_libraries(lapack ${BLAS_LIBRARIES} f2c)
+target_link_libraries(lapack ${BLAS_LIBRARIES})
 
 install(TARGETS lapack 
         RUNTIME DESTINATION bin
--- a/F2CLIBS/libf2c/CMakeLists.txt
+++ b/F2CLIBS/libf2c/CMakeLists.txt
@@ -58,5 +58,5 @@
 endif()
 include_directories(${CLAPACK_SOURCE_DIR}/F2CLIBS/libf2c)
 include_directories(${CLAPACK_BINARY_DIR}/F2CLIBS/libf2c)
-add_library(f2c STATIC ${OFILES} ${CMAKE_CURRENT_BINARY_DIR}/arith.h)
+add_library(f2c OBJECT ${OFILES} ${CMAKE_CURRENT_BINARY_DIR}/arith.h)
 set_property(TARGET f2c PROPERTY PREFIX lib)
