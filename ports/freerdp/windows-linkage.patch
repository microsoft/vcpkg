diff --git a/CMakeLists.txt b/CMakeLists.txt
index 3918bd2..7652f13 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -379,6 +379,9 @@ if(WIN32)
 endif()
 
 add_compile_definitions(FREERDP_EXPORTS)
+if(NOT BUILD_SHARED_LIBS)
+	add_definitions(-DRDTK_EXPORTS)
+endif()
 
 # Mac OS X
 if(APPLE)
