@PACKAGE_INIT@

if(NOT TARGET mapbox::variant)
    add_library(mapbox::variant INTERFACE IMPORTED)
    set_target_properties(mapbox::variant PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "@CMAKE_INSTALL_PREFIX@/@CMAKE_INSTALL_INCLUDEDIR@"
        INTERFACE_COMPILE_FEATURES "cxx_std_11"
    )
    if(APPLE)
        set_property(TARGET mapbox::variant APPEND PROPERTY
            INTERFACE_LINK_OPTIONS "-framework CoreFoundation"
        )
    endif()
endif()

find_dependency(Boost CONFIG REQUIRED COMPONENTS timer system chrono)

if(UNIX AND NOT APPLE)
    set_property(TARGET mapbox::variant APPEND PROPERTY
        INTERFACE_LINK_LIBRARIES "rt"
    )
endif()

check_required_components(variant)
