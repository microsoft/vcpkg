diff --git a/CMakeLists.txt b/CMakeLists.txt
index ff684a8..7780c6a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -5,8 +5,13 @@ if(NOT MSVC)
 	message(FATAL_ERROR "Please use configure and make, this cmake file is currently only to generate msvc solution files")
 endif()
 
-#To force the compiler to use C++ 11
-set(CMAKE_CXX_STANDARD 11)
+if(NOT MSVC)
+	# To force the compiler to use C++ 11
+	set(CMAKE_CXX_STANDARD 11)
+else()
+	# riff.cpp has a line which forces C++ 20 on MSVC, other compilers not affected
+	set(CMAKE_CXX_STANDARD 20)
+endif()
 set(CMAKE_CXX_STANDARD_REQUIRED TRUE)
 
 
