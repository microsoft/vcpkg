The package tree-sitter-bash can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_bash REQUIRED IMPORTED_TARGET tree-sitter-bash)

    target_link_libraries(main PRIVATE PkgConfig::ts_bash)

