set(tensorflow_cc_FOUND TRUE)

add_library(tensorflow_cc::tensorflow_cc-part1 STATIC IMPORTED)
set_target_properties(tensorflow_cc::tensorflow_cc-part1
	PROPERTIES
	IMPORTED_LOCATION "${_VCPKG_INSTALLED_DIR}/${TARGET_TRIPLET}/lib/tensorflow.lib"
	INTERFACE_INCLUDE_DIRECTORIES
		"${_VCPKG_INSTALLED_DIR}/${TARGET_TRIPLET}/include/tensorflow-external"
		"${_VCPKG_INSTALLED_DIR}/${TARGET_TRIPLET}/include/tensorflow-external/src"
)
