add_library(tensorflow_cc::tensorflow_cc SHARED IMPORTED)
set_target_properties(tensorflow_cc::tensorflow_cc
	PROPERTIES
	IMPORTED_LOCATION "${_VCPKG_INSTALLED_DIR}/${TARGET_TRIPLET}/bin/tensorflow.dll"
	IMPORTED_IMPLIB "${_VCPKG_INSTALLED_DIR}/${TARGET_TRIPLET}/bin/tensorflow.lib"
	INTERFACE_INCLUDE_DIRECTORIES
		"${_VCPKG_INSTALLED_DIR}/${TARGET_TRIPLET}/include/tensorflow-external"
		"${_VCPKG_INSTALLED_DIR}/${TARGET_TRIPLET}/include/tensorflow-external/src"
)
set(tensorflow_cc_FOUND TRUE)
