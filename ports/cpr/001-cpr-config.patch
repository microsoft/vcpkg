diff --git a/cpr/CMakeLists.txt b/cpr/CMakeLists.txt
index d43fc1d..b2ff9a6 100644
--- a/cpr/CMakeLists.txt
+++ b/cpr/CMakeLists.txt
@@ -26,10 +26,15 @@ target_link_libraries(cpr PUBLIC CURL::libcurl) # todo should be private, but fi
 
 # Set version for shared libraries.
 set_target_properties(cpr
-     PROPERTIES
+     PROPERTIES 
      VERSION ${${PROJECT_NAME}_VERSION}
      SOVERSION ${${PROJECT_NAME}_VERSION_MAJOR})
 
+if (NOT DISABLE_INSTALL_HEADERS)
+  install(DIRECTORY ${CMAKE_CURRENT_LIST_DIR}/../include/cpr DESTINATION include)
+endif()
+ 
+
 # Import GNU common install directory variables
 include(GNUInstallDirs)
 
@@ -37,6 +42,7 @@ if(CPR_FORCE_USE_SYSTEM_CURL)
     install(TARGETS cpr
             EXPORT cprTargets
             ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
+            RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
             LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR})
 
     # Include CMake helpers for package config files
