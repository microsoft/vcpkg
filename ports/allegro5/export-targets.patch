diff --git a/CMakeLists.txt b/CMakeLists.txt
index 7c5720b..04d18c7 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1156,6 +1156,18 @@ if(INSTALL_PKG_CONFIG_FILES)
     endforeach(versuffix)
 endif(INSTALL_PKG_CONFIG_FILES)
 
+file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/unofficial-allegro5-config.cmake"
+[[
+include("${CMAKE_CURRENT_LIST_DIR}/unofficial-allegroTargets.cmake")
+include("${CMAKE_CURRENT_LIST_DIR}/unofficial-allegro_audioTargets.cmake")
+file(GLOB TARGET_FILES "${CMAKE_CURRENT_LIST_DIR}/*Targets.cmake")
+foreach (TARGET_FILE ${TARGET_FILES})
+    include("${TARGET_FILE}")
+endforeach()
+
+]])
+install(FILES "${CMAKE_CURRENT_BINARY_DIR}/unofficial-allegro5-config.cmake" DESTINATION share/unofficial-allegro5)
+
 #-----------------------------------------------------------------------------#
 #
 #   Documentation
diff --git a/cmake/Common.cmake b/cmake/Common.cmake
index 74edef8..6311067 100644
--- a/cmake/Common.cmake
+++ b/cmake/Common.cmake
@@ -220,6 +220,7 @@ endfunction(set_our_framework_properties)
 
 function(install_our_library target filename)
     install(TARGETS ${target}
+            EXPORT unofficial-${target}Targets
             LIBRARY DESTINATION "lib${LIB_SUFFIX}"
             ARCHIVE DESTINATION "lib${LIB_SUFFIX}"
             FRAMEWORK DESTINATION "${FRAMEWORK_INSTALL_PREFIX}"
@@ -227,6 +228,14 @@ function(install_our_library target filename)
             # Doesn't work, see below.
             # PUBLIC_HEADER DESTINATION "include"
             )
+    export(EXPORT unofficial-${target}Targets
+           NAMESPACE unofficial::allegro5::
+           FILE "${CMAKE_CURRENT_BINARY_DIR}/unofficial-${target}Targets.cmake"
+    )
+    install(EXPORT unofficial-${target}Targets
+        NAMESPACE unofficial::allegro5::
+        DESTINATION share/unofficial-allegro5
+    )
     if(0)
         install(FILES ${CMAKE_BINARY_DIR}/lib/${filename}.pdb
             DESTINATION lib
