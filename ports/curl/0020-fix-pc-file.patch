diff --git a/CMakeLists.txt b/CMakeLists.txt
index f9a2d73..6ca43f9 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1459,7 +1459,7 @@ set(includedir              "\${prefix}/include")
 set(LDFLAGS                 "${CMAKE_SHARED_LINKER_FLAGS}")
 set(LIBCURL_LIBS            "")
 set(libdir                  "${CMAKE_INSTALL_PREFIX}/lib")
-foreach(_lib ${CMAKE_C_IMPLICIT_LINK_LIBRARIES} ${CURL_LIBS})
+foreach(_lib ${CURL_LIBS})
   if(TARGET "${_lib}")
     set(_libname "${_lib}")
     get_target_property(_libtype "${_libname}" TYPE)
@@ -1476,6 +1476,13 @@ foreach(_lib ${CMAKE_C_IMPLICIT_LINK_LIBRARIES} ${CURL_LIBS})
       continue()
     endif()
   endif()
+  if(_lib MATCHES "^(.*)/([^/]*)[.]framework$")
+    if(CMAKE_MATCH_1 IN_LIST CMAKE_C_IMPLICIT_LINK_FRAMEWORK_DIRECTORIES)
+      set(_lib "-framework ${CMAKE_MATCH_2}")
+    else()
+      set(_lib "-framework ${_lib}")
+    endif()
+  endif()
   if(_lib MATCHES ".*/.*" OR _lib MATCHES "^-")
     set(LIBCURL_LIBS          "${LIBCURL_LIBS} ${_lib}")
   else()
