diff --git a/CMakeLists.txt b/CMakeLists.txt
index 9fd4e58..f62cbc7 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -99,3 +99,5 @@ if(NOT TARGET uninstall)
     uninstall COMMAND ${CMAKE_COMMAND} -P
                       ${CMAKE_CURRENT_BINARY_DIR}/cmake_uninstall.cmake)
 endif()
+
+install(DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Data" DESTINATION share/cgal)
diff --git a/Installation/lib/cmake/CGAL/CGALConfig.cmake b/Installation/lib/cmake/CGAL/CGALConfig.cmake
index 4f3a16b..581de43 100644
--- a/Installation/lib/cmake/CGAL/CGALConfig.cmake
+++ b/Installation/lib/cmake/CGAL/CGALConfig.cmake
@@ -77,8 +77,8 @@ if (NOT CGAL_DATA_DIR)
   if(DEFINED ENV{CGAL_DATA_DIR})
     set(CGAL_DATA_DIR $ENV{CGAL_DATA_DIR})
   else()
-    if (EXISTS "${CGAL_ROOT}/Data/data")
-      set(CGAL_DATA_DIR "${CGAL_ROOT}/Data/data")
+    if (EXISTS "${CGAL_ROOT}/share/cgal/Data/data")
+      set(CGAL_DATA_DIR "${CGAL_ROOT}/share/cgal/Data/data")
     else()
       if (EXISTS "${CGAL_ROOT}/data")
         set(CGAL_DATA_DIR "${CGAL_ROOT}/data")
