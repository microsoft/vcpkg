toomanycooks provides CMake targets:

  set(TMC_PRIORITY_COUNT 8) # integer from 1 to 16, optional
  find_package(TooManyCooks CONFIG REQUIRED)
  target_link_libraries(main PRIVATE TooManyCooks::TooManyCooks)
