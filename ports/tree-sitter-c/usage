The package tree-sitter-c can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_c REQUIRED IMPORTED_TARGET tree-sitter-c)

    target_link_libraries(main PRIVATE PkgConfig::ts_c)

