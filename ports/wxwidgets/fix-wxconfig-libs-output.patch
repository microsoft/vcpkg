diff --git a/build/cmake/config.cmake b/build/cmake/config.cmake
index 91d11ac..d8f26d7 100644
--- a/build/cmake/config.cmake
+++ b/build/cmake/config.cmake
@@ -42,6 +42,21 @@ macro(wx_get_dependencies var lib)
                 set(dep_name "-l${dep_name}")
             else()
                 get_filename_component(dep_name ${dep} NAME)
+                string(REGEX MATCH "^-.*$" ADDED_PREFIX ${dep_name})
+                if (ADDED_PREFIX)
+                    # Do nothing
+                else()
+                    string(REGEX MATCH "^lib(.*).a$" CURR_STATIC_NAME ${dep_name})
+                    string(REGEX MATCH "^lib(.*).so$" CURR_DYNAMIC_NAME ${dep_name})
+                    if (CURR_STATIC_NAME)
+                        set(dep_name "-l${CURR_STATIC_NAME}")
+                    elseif (CURR_DYNAMIC_NAME)
+                        set(dep_name "-l${CURR_DYNAMIC_NAME}")
+                    else()
+                        set(dep_name "-l${dep_name}")
+                    endif()
+                endif()
+
             endif()
             wx_string_append(${var} "${dep_name} ")
         endforeach()
