The package serd can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(serd REQUIRED IMPORTED_TARGET serd-0)
    target_link_libraries(main PRIVATE PkgConfig::serd)
