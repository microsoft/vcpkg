diff --git a/CMakeLists.txt b/CMakeLists.txt
index f23dd5aa..b0d128d6 100644
--- a/CMakeLists.txt	
+++ b/CMakeLists.txt
@@ -315,8 +315,10 @@ ENDIF(CMAKE_COMPILER_IS_GNUCXX)
 FIND_PACKAGE(ZLIB REQUIRED)
 MESSAGE("Found zlib headers in ${ZLIB_INCLUDE_DIR}, library at ${ZLIB_LIBRARIES}")
 
-FIND_PACKAGE(LIBCRYPTO)
-
+FIND_PACKAGE(OpenSSL)
+set(LIBCRYPTO_INCLUDE_DIR ${OPENSSL_INCLUDE_DIR})
+set(LIBCRYPTO_LIBRARIES OpenSSL::Crypto)
+set(LIBCRYPTO_FOUND ${OPENSSL_FOUND})
 IF(LIBCRYPTO_FOUND)
 	SET(PODOFO_HAVE_OPENSSL TRUE)
 	INCLUDE_DIRECTORIES(${LIBCRYPTO_INCLUDE_DIR})
@@ -339,7 +341,10 @@ ELSE(LIBIDN_FOUND)
 	MESSAGE("Libidn not found. AES-256 Encryption support will be disabled")
 ENDIF(LIBIDN_FOUND)
 
-FIND_PACKAGE(LIBJPEG)
+FIND_PACKAGE(JPEG)
+set(LIBJPEG_INCLUDE_DIR ${JPEG_INCLUDE_DIR})
+set(LIBJPEG_LIBRARIES ${JPEG_LIBRARIES})
+set(LIBJPEG_FOUND ${JPEG_FOUND})
 
 IF(LIBJPEG_FOUND)
   MESSAGE("Found libjpeg headers in ${LIBJPEG_INCLUDE_DIR}, library at ${LIBJPEG_LIBRARIES}")
