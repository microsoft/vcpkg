The package opendis6 provides CMake targets:

	find_package(OpenDIS CONFIG REQUIRED)
	target_include_directories(main PRIVATE ${OpenDIS_INCLUDE_DIR})
	target_link_libraries(main PRIVATE OpenDIS::OpenDIS6)
