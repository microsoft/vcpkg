diff --git a/configure.ac b/configure.ac
index bc9acc914..d267d2b57 100644
--- a/configure.ac
+++ b/configure.ac
@@ -2457,8 +2457,8 @@ return 0;
                   LDFLAGS=$OLD_LDFLAGS
                   LIBS=$OLD_LIBS
                 else
-                  LIB_PNG="-l${pnglib}"
-                  pkg_config_to_flags 'libpng' "${LIB_PNG}" ''
+                  # LIB_PNG="-l${pnglib}"
+                  # pkg_config_to_flags 'libpng' "${LIB_PNG}" ''
                   #LIBS="$LIB_PNG $LIBS"
                   AC_DEFINE([HasPNG],[1],[Define if you have PNG library])
                   AC_MSG_RESULT([yes])
