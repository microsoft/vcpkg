diff --git a/configure.ac b/configure.ac
index 1fd5c97..8b24b60 100644
--- a/configure.ac
+++ b/configure.ac
@@ -49,7 +49,12 @@ AC_PROG_YACC
 AC_ARG_ENABLE(doc,
   AS_HELP_STRING([--disable-doc], [don't generate any documentation]),
     enable_doc=$enableval, enable_doc=yes)
-AM_CONDITIONAL(ENABLE_DOC, test "$enable_doc" != "no")
+AM_CONDITIONAL(ENABLE_DOC, false)
+AM_CONDITIONAL(ENABLE_GTK_DOC, false)
+AM_CONDITIONAL(GTK_DOC_USE_LIBTOOL, false)
+AM_CONDITIONAL(GTK_DOC_BUILD_HTML, false)
+AM_CONDITIONAL(GTK_DOC_BUILD_PDF, false)
+AM_CONDITIONAL(HAVE_GTK_DOC, false)
 
 AC_CHECK_FUNCS([clock_gettime fmemopen])
 
@@ -58,7 +63,7 @@ AM_MISSING_PROG(HELP2MAN, help2man, $missing_dir)
 AC_LIBTOOL_WIN32_DLL
 AM_PROG_AR
 AM_PROG_LIBTOOL
-GTK_DOC_CHECK(1.2)
+#GTK_DOC_CHECK(1.2)
 
 AC_CHECK_SIZEOF(unsigned long int, 4)
 AC_CHECK_SIZEOF(unsigned int, 4)
diff --git a/gtk-doc.make b/gtk-doc.make
index 7d9a27f..f57f635 100644
--- a/gtk-doc.make
+++ b/gtk-doc.make
@@ -268,7 +268,7 @@ maintainer-clean-local:
 
 install-data-local:
 	@installfiles=`echo $(builddir)/html/*`; \
-	if test "$$installfiles" = '$(builddir)/html/*'; \
+	if true; \
 	then echo 1>&2 'Nothing to install' ; \
 	else \
 	  if test -n "$(DOC_MODULE_VERSION)"; then \
@@ -316,6 +316,5 @@ dist-hook: dist-check-gtkdoc all-gtk-doc dist-hook-local
 	@-cp ./$(DOC_MODULE).types $(distdir)/
 	@-cp ./$(DOC_MODULE)-sections.txt $(distdir)/
 	@cd $(distdir) && rm -f $(DISTCLEANFILES)
-	@$(GTKDOC_REBASE) --online --relative --html-dir=$(distdir)/html
 
 .PHONY : dist-hook-local docs
