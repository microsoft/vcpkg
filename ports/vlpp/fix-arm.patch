diff --git a/Import/VlppOS.Windows.cpp b/Import/VlppOS.Windows.cpp
index 5bcb304..54c84cf 100644
--- a/Import/VlppOS.Windows.cpp
+++ b/Import/VlppOS.Windows.cpp
@@ -1779,7 +1779,12 @@ SpinLock
 	{
 		while(_InterlockedCompareExchange(&token, 1, 0)!=0)
 		{
-			while(token!=0) _mm_pause();
+            while (token != 0)
+#if defined(__arm__) || defined(_M_ARM) || defined(_M_ARM64)
+              __yield();
+#else
+              _mm_pause();
+#endif
 		}
 	}
 
