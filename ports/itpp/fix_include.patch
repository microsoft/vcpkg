diff --git a/itpp/CMakeLists.txt b/itpp/CMakeLists.txt
index e8c78d3..2aa549c 100644
--- a/itpp/CMakeLists.txt
+++ b/itpp/CMakeLists.txt
@@ -42,8 +42,10 @@ include_directories ( ${CMAKE_BINARY_DIR}
                       ${CMAKE_SOURCE_DIR}
                       ${BLAS_INCLUDES}
                       ${LAPACK_INCLUDES}
-                      ${FFT_INCLUDES}
                     )
+if(FFT_FOUND)
+    include_directories(${FFT_INCLUDES})
+endif()
 
 if (ITPP_SHARED_LIB)
   add_library (${libitpp_target} SHARED ${ITPP_SRCS})
