diff --git a/cmake/CMakeLists.txt b/cmake/CMakeLists.txt
index 7bb1eed..38796bd 100644
--- a/cmake/CMakeLists.txt
+++ b/cmake/CMakeLists.txt
@@ -254,7 +254,8 @@ endif()
 if(USD_SUPPORT)
     UNIFY_ENVVARS_AND_CACHE(USD_INCLUDE_DIR)
     UNIFY_ENVVARS_AND_CACHE(USD_LIBRARY_DIR)
-
+    find_package(pxr CONFIG REQUIRED)
+    if(0)
     if("${USD_INCLUDE_DIR}" STREQUAL "")
         find_path(USD_INCLUDE_DIR pxr.h
             PATHS
@@ -271,7 +272,7 @@ if(USD_SUPPORT)
         set(USD_INCLUDE_DIR ${USD_INCLUDE_DIR} CACHE FILEPATH "USD header files")
         message(STATUS "Looking for USD include files in: ${USD_INCLUDE_DIR}")
     endif()
-
+    endif()
     set(USD_LIBRARIES
             usd_usd 
             usd_usdGeom
@@ -369,6 +370,9 @@ if(BUILD_IFCGEOM)
 
     # Open CASCADE
     if(WITH_OPENCASCADE)
+    find_package(OpenCASCADE CONFIG REQUIRED)
+    set(OCC_VERSION_STRING ${OpenCASCADE_VERSION})
+    if (0)
     if("${OCC_INCLUDE_DIR}" STREQUAL "")
 	clear_wasm_sysroot()
         find_path(OCC_INCLUDE_DIR Standard_Version.hxx
@@ -389,7 +393,6 @@ if(BUILD_IFCGEOM)
         set(OCC_INCLUDE_DIR ${OCC_INCLUDE_DIR} CACHE FILEPATH "Open CASCADE header files")
         message(STATUS "Looking for Open CASCADE include files in: ${OCC_INCLUDE_DIR}")
     endif()
-
     if(OCC_INCLUDE_DIR)
         file(STRINGS ${OCC_INCLUDE_DIR}/Standard_Version.hxx OCC_MAJOR
             REGEX "#define OCC_VERSION_MAJOR.*"
@@ -405,6 +408,7 @@ if(BUILD_IFCGEOM)
         string(REGEX MATCH "[0-9]+" OCC_MAINT ${OCC_MAINT})
         set(OCC_VERSION_STRING "${OCC_MAJOR}.${OCC_MINOR}.${OCC_MAINT}")
     endif(OCC_INCLUDE_DIR)
+    endif()
 
     set(OPENCASCADE_LIBRARY_NAMES
         TKernel TKMath TKBRep TKGeomBase TKGeomAlgo TKG3d TKG2d TKShHealing TKTopAlgo TKMesh TKPrim TKBool TKBO
@@ -584,6 +588,14 @@ if(COLLADA_SUPPORT)
 endif(COLLADA_SUPPORT)
 
 if(HDF5_SUPPORT)
+    find_package(hdf5 CONFIG REQUIRED)
+    if(BUILD_SHARED_LIBS)
+        set(HDF5_LIBRARIES hdf5::hdf5-shared hdf5::hdf5_cpp-shared)
+    else()
+        set(HDF5_LIBRARIES hdf5::hdf5-static hdf5::hdf5_cpp-static)
+    endif(BUILD_SHARED_LIBS)
+    
+    if(0)
     if("${HDF5_INCLUDE_DIR}" STREQUAL "")
         message(STATUS "No HDF5 include directory specified")
     else()
@@ -658,7 +670,7 @@ if(HDF5_SUPPORT)
             z dl
         )
     endif()
-
+    endif()
     add_definitions(-DWITH_HDF5)
     set(SWIG_DEFINES ${SWIG_DEFINES} -DWITH_HDF5)
 endif(HDF5_SUPPORT)
@@ -942,6 +954,9 @@ endif()
 
 if(BUILD_IFCGEOM)
     if(WITH_CGAL)
+    find_package(CGAL CONFIG REQUIRED)
+    set(CGAL_LIBRARIES CGAL::CGAL)
+    if(0)
 	clear_wasm_sysroot()
         find_library(libGMP NAMES gmp mpir PATHS ${GMP_LIBRARY_DIR} NO_DEFAULT_PATH)
         find_library(libMPFR NAMES mpfr PATHS ${MPFR_LIBRARY_DIR} NO_DEFAULT_PATH)
@@ -956,6 +971,7 @@ if(BUILD_IFCGEOM)
         list(APPEND CGAL_LIBRARIES "${libMPFR}")
         list(APPEND CGAL_LIBRARIES "${libGMP}")
     endif()
+    endif()
 
     foreach(kernel ${GEOMETRY_KERNELS})
         string(TOUPPER ${kernel} KERNEL_UPPER)
@@ -1208,7 +1224,7 @@ if(BUILD_IFCMAX)
     add_subdirectory(../src/ifcmax ifcmax)
 endif()
 
-if(WITH_CGAL)
+if(0)
     add_subdirectory(../src/svgfill svgfill)        
 endif()
 
