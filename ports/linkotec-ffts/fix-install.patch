diff --git a/CMakeLists.txt b/CMakeLists.txt
index 748f412..f38a060 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -500,6 +500,8 @@ if(GENERATE_POSITION_INDEPENDENT_CODE)
   set(CMAKE_POSITION_INDEPENDENT_CODE ON)
 endif(GENERATE_POSITION_INDEPENDENT_CODE)
 
+include(GNUInstallDirs)
+
 if(ENABLE_SHARED)
   add_library(ffts_shared SHARED
     ${FFTS_HEADERS}
@@ -513,7 +515,10 @@ if(ENABLE_SHARED)
     VERSION ${FFTS_MAJOR}.${FFTS_MINOR}.${FFTS_MICRO}
   )
 
-  install( TARGETS ffts_shared DESTINATION ${LIB_INSTALL_DIR} )
+  install( TARGETS ffts_shared
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR})
 endif(ENABLE_SHARED)
 
 if(ENABLE_STATIC)
@@ -527,7 +532,10 @@ if(ENABLE_STATIC)
     set_target_properties(ffts_static PROPERTIES OUTPUT_NAME ffts)
   endif(UNIX)
 
-  install( TARGETS ffts_static DESTINATION ${LIB_INSTALL_DIR} )
+  install( TARGETS ffts_static DESTINATION 
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR})
 endif(ENABLE_STATIC)
 
 if(ENABLE_STATIC OR ENABLE_SHARED)
@@ -535,7 +543,10 @@ if(ENABLE_STATIC OR ENABLE_SHARED)
     NAMES mpfr.h
     PATHS ${INCLUDE_INSTALL_DIR}
   )
-  find_library(MPFR_LIBRARIES mpfr PATHS ${LIB_INSTALL_DIR})
+  find_library(MPFR_LIBRARIES mpfr PATHS 
+    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+    ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR})
   find_package(OpenMP)
 
   if(MPFR_INCLUDES)
