inlined-vector provides CMake targets:

  find_package(inlined-vector CONFIG REQUIRED)
  target_link_libraries(main PRIVATE inlined-vector::inlined-vector)

Or simply include the header directly (header-only):

  #include <inlined_vector.hpp>

  lloyal::InlinedVector<int, 16> vec;
  vec.push_back(42);
