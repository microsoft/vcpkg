diff --git a/PC/pyconfig.h.in b/PC/pyconfig.h.in
index 424421f..7e998fa 100644
--- a/PC/pyconfig.h.in
+++ b/PC/pyconfig.h.in
@@ -311,7 +311,7 @@ Py_NO_ENABLE_SHARED to find out.  Also support MS_NO_COREDLL for b/w compat */
 #ifdef MS_COREDLL
 #       if !defined(Py_BUILD_CORE) && !defined(Py_BUILD_CORE_BUILTIN)
                 /* not building the core - must be an ext */
-#               if defined(_MSC_VER)
+#               if defined(_MSC_VER) && !defined(Py_NO_LINK_LIB)
                         /* So MSVC users need not specify the .lib
                         file in their Makefile */
 #                       if defined(Py_GIL_DISABLED)
