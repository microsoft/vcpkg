The package tree-sitter-rust can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_rust REQUIRED IMPORTED_TARGET tree-sitter-rust)

    target_link_libraries(main PRIVATE PkgConfig::ts_rust)

