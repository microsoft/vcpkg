diff --git a/BUILD.gn b/BUILD.gn
index c6966d249d..9f7a0ecc56 100644
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -23,10 +23,12 @@ config("skia_public") {
   defines = [
     "SK_CODEC_DECODES_BMP",
     "SK_CODEC_DECODES_WBMP",
-
-    # TODO(kjlubick,fmalita) Remove when clients updated and APIs deleted.
-    "SK_HIDE_PATH_EDIT_METHODS",
   ]
+  if (!skia_enable_pathediting) {
+    # TODO(kjlubick,fmalita) Remove when clients updated and APIs deleted.
+    defines += [ "SK_HIDE_PATH_EDIT_METHODS" ]
+  }
+
   cflags_objcc = []
   if (is_component_build) {
     defines += [ "SKIA_DLL" ]
diff --git a/gn/skia.gni b/gn/skia.gni
index ee62fca9b7..ee01b7aeba 100644
--- a/gn/skia.gni
+++ b/gn/skia.gni
@@ -21,6 +21,7 @@ declare_args() {
   skia_enable_fontmgr_win = is_win
   skia_enable_gpu_debug_layers = is_skia_dev_build && is_debug
   skia_enable_optimize_size = false
+  skia_enable_pathediting = false
   skia_enable_pdf = !is_wasm
   skia_enable_precompile = true
   skia_enable_skottie = !is_component_build
