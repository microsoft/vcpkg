The package thorvg can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(thorvg-1 REQUIRED IMPORTED_TARGET thorvg-1)
    target_link_libraries(main PRIVATE PkgConfig::thorvg-1)
