The package tree-sitter-json can be imported via CMake FindPkgConfig module:

    find_package(PkgConfig)
    pkg_check_modules(ts_json REQUIRED IMPORTED_TARGET tree-sitter-json)

    target_link_libraries(main PRIVATE PkgConfig::ts_json)

