diff --git a/CMakeLists.txt b/CMakeLists.txt
index bd79a62..7dfcc18 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -277,7 +277,7 @@ if (STDEXEC_IS_TOP_LEVEL)
   # Integrate with LLVM/clang tooling
   include(${CMAKE_CURRENT_SOURCE_DIR}/cmake/clangd_compile_info.cmake)
   add_executable(_clangd_helper_file
-    include/._clangd_helper_file.cpp
+    ${CMAKE_CURRENT_SOURCE_DIR}/include/._clangd_helper_file.cpp
   )
 endif()
 
@@ -472,6 +472,9 @@ endif()
 include(CPack)
 
 install(TARGETS stdexec system_context
+  RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
+  LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
+  ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}
 	EXPORT stdexec-exports
 	FILE_SET headers
 	FILE_SET version_config)
