CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(VCPKG_WRAPPER)

LIST(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/vcpkg/scripts")
LIST(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/vcpkg/scripts/cmake")

include(${CMAKE_CURRENT_SOURCE_DIR}/conanbuildinfo.cmake)
CONAN_BASIC_SETUP()

SET(BUILDTREES_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
SET(CURRENT_PORT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/vcpkg/ports/${PORT}")
include("ports")

